# 🔍 401 錯誤診斷指南

## 錯誤訊息
```
{"code":401,"message":"Missing authorization header"}
```

## 診斷步驟（已實施）

### ✅ 步驟 1: 完整輸出所有 Headers

**位置**: `supabase/functions/line-auth/index.ts`

**實施內容**:
- 完整輸出所有 request headers（JSON 格式）
- 用於確認 Authorization header 是否真的被傳遞

**日誌標記**: `[DIAGNOSTIC] All request headers:`

---

### ✅ 步驟 2: 檢查 Authorization Header 格式

**位置**: `supabase/functions/line-auth/index.ts`

**檢查項目**:
1. Header 是否存在
2. 是否包含 `Bearer` 前綴
3. Token 格式是否正確

**正確格式**:
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

**錯誤格式**:
```
Authorization: eyJhbGciOiJIUzI1NiIs...  ❌ 缺少 Bearer 前綴
authorization: Bearer eyJhbGciOiJIUzI1NiIs...  ❌ 大小寫錯誤
```

**日誌標記**: `[DIAGNOSTIC] Authorization header analysis:`

---

### ✅ 步驟 3: 確認使用 Supabase Session Token

**位置**: `src/pages/AuthPage.tsx`

**檢查項目**:
1. 調用 Edge Function 前是否有 Supabase session
2. `supabase.functions.invoke` 是否使用正確的 session token
3. Token 是否為 Supabase JWT（而非 LINE token）

**重要**:
- `supabase.functions.invoke` 會自動使用當前 session 的 `access_token`
- 如果沒有 session，它會使用 `anon` key（可能導致 401）
- 對於 LINE 登入，用戶可能還沒有 session，這是正常的

**日誌標記**: `[AuthPage] ✅ Active Supabase session found:`

---

### ✅ 步驟 4: 確認 Session 正確設置

**位置**: `src/components/OAuthCallbackHandler.tsx`

**檢查項目**:
1. `verifyOtp` 是否返回 session
2. Session 是否正確設置到 Supabase client
3. `getSession()` 是否能獲取到 session

**實施內容**:
- 驗證 `verifyOtp` 返回的 session
- 確認 session 已設置（使用 `getSession()` 驗證）
- 如果 `verifyOtp` 返回 session 但 `getSession()` 返回 null，手動設置 session

**日誌標記**: `[OAuthCallbackHandler] ✅ Confirmed: Supabase session is active`

---

### ✅ 步驟 5: 可選 - 暫時關閉 JWT 驗證（僅用於診斷）

**位置**: `supabase/functions/line-auth/index.ts`

**環境變數**:
```bash
DISABLE_JWT_VERIFY=true
```

**用途**:
- 用於診斷是否為 JWT 驗證層的問題
- 如果關閉 JWT 驗證後 401 錯誤消失，則問題在 JWT 驗證層
- 如果關閉後仍有 401，則問題在其他地方

**⚠️ 警告**:
- 僅用於開發/測試環境
- 生產環境請務必啟用 JWT 驗證
- 測試完成後務必還原

---

## 常見原因與解決方案

### 🧩 原因 A：Authorization Header 格式錯誤

**症狀**:
- 日誌顯示 `isBearerFormat: false`
- 或 `Authorization header missing "Bearer " prefix!`

**解決方案**:
1. 確認 `supabase.functions.invoke` 自動添加的 header 格式正確
2. 如果手動調用，確保格式為：`Authorization: Bearer <token>`

---

### 🧩 原因 B：Token 類型錯誤

**症狀**:
- 傳遞的是 LINE access token，而非 Supabase JWT
- Token 已過期

**解決方案**:
1. 確認調用 Edge Function 前已建立 Supabase session
2. 使用 `supabase.auth.getSession()` 獲取正確的 Supabase JWT
3. 確保 `verifyOtp` 後 session 已正確設置

---

### 🧩 原因 C：環境變數配置錯誤

**症狀**:
- `SERVICE_ROLE_KEY` 配置錯誤
- Edge Function 無法驗證 JWT

**解決方案**:
1. 檢查 Supabase Dashboard 中的環境變數
2. 確認 `SERVICE_ROLE_KEY` 值正確
3. 重新部署 Edge Function

---

## 診斷流程

### 1. 查看日誌

重新測試 LINE 登入，查看 Edge Function 日誌：

```
[DIAGNOSTIC] All request headers: {...}
[DIAGNOSTIC] Authorization header analysis: {...}
```

### 2. 檢查 Header 格式

確認：
- ✅ Header 存在
- ✅ 包含 `Bearer` 前綴
- ✅ Token 格式正確

### 3. 檢查 Session

確認：
- ✅ 調用 Edge Function 前有 Supabase session（如果需要的話）
- ✅ `verifyOtp` 後 session 已設置
- ✅ `getSession()` 能獲取到 session

### 4. 測試 JWT 驗證（可選）

設置 `DISABLE_JWT_VERIFY=true`，測試：
- 如果 401 錯誤消失 → 問題在 JWT 驗證層
- 如果仍有 401 → 問題在其他地方

---

## 下一步行動

1. **重新測試 LINE 登入**
   - 查看 Edge Function 日誌中的 `[DIAGNOSTIC]` 標記
   - 確認 Authorization header 格式

2. **根據診斷結果修復**
   - 如果 Header 格式錯誤 → 修復格式
   - 如果 Token 類型錯誤 → 確保使用 Supabase JWT
   - 如果 JWT 驗證問題 → 檢查環境變數

3. **驗證修復**
   - 重新測試 LINE 登入
   - 確認 401 錯誤已解決
   - 確認登入流程正常

---

**最後更新**: 2025-01-27
**狀態**: ✅ 已修復 - GET callback 請求現在在最前面處理，避免觸發 JWT 驗證

---

## ✅ 已修復的問題

### 問題：GET callback 請求返回 401

**根本原因**:
- LINE 服務器直接重定向到 Edge Function 的 callback URL（GET 請求）
- GET 請求不會包含 Supabase 的 Authorization header
- 即使使用 `Deno.serve`，Supabase 運行時可能在代碼執行前檢查 JWT

**解決方案**:
- ✅ 將 GET callback 請求的處理移到最前面（在任何日誌或驗證之前）
- ✅ 立即返回重定向響應，避免觸發任何 JWT 驗證
- ✅ 確保 GET callback 請求不會被 JWT 驗證攔截

**修復位置**: `supabase/functions/line-auth/index.ts`

**修復內容**:
```typescript
Deno.serve(async (req) => {
  // ⚠️ 關鍵修復：GET callback 請求必須在最前面處理
  const url = new URL(req.url)
  const path = url.pathname
  const isCallback = path.endsWith('/callback') || path.endsWith('/callback/')
  
  // ✅ 優先處理 GET callback 請求（LINE 回調，沒有 Authorization header）
  if (isCallback && req.method === 'GET' && !req.headers.get('authorization')) {
    // 立即返回重定向，避免觸發 JWT 驗證
    return Response.redirect(frontendCallbackUrl.toString(), 302)
  }
  
  // ... 其他處理 ...
})
```

**測試建議**:
1. 重新測試 LINE 登入
2. 確認 GET callback 請求不再返回 401
3. 確認登入流程正常
