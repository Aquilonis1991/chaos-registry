# Edge Function 503 錯誤根本修復

## 🔍 問題根源

OPTIONS 請求返回 **503 狀態碼**，表示 Edge Function 根本沒有正常啟動。

### 根本原因

1. **使用 `!` 斷言運算符**: 在模組載入時使用 `!` 斷言運算符（如 `Deno.env.get('SUPABASE_URL')!`）
   - 如果環境變數為 `undefined`，`!` 不會立即拋出錯誤
   - 但在實際使用時，如果值為 `undefined`，會導致運行時錯誤
   - 這可能導致 Edge Function 在啟動時就失敗

2. **模組載入時的錯誤**: 如果環境變數缺失，某些操作（如 `substring()`）會導致運行時錯誤
   - 例如：`SERVICE_ROLE_KEY.substring(0, 32)` 如果 `SERVICE_ROLE_KEY` 為 `undefined`，會導致錯誤

## ✅ 修復方案

### 1. 移除 `!` 斷言運算符

**修改前**:
```typescript
const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!
const SUPABASE_ANON_KEY = Deno.env.get('SUPABASE_ANON_KEY')!
const SERVICE_ROLE_KEY = Deno.env.get('SERVICE_ROLE_KEY')!
```

**修改後**:
```typescript
const SUPABASE_URL = Deno.env.get('SUPABASE_URL') || ''
const SUPABASE_ANON_KEY = Deno.env.get('SUPABASE_ANON_KEY') || ''
const SERVICE_ROLE_KEY = Deno.env.get('SERVICE_ROLE_KEY') || ''
```

### 2. 修復 STATE_SECRET 生成

**修改前**:
```typescript
const STATE_SECRET = SERVICE_ROLE_KEY.substring(0, 32)
```

**修改後**:
```typescript
// 如果 SERVICE_ROLE_KEY 為空，使用默認值（僅用於開發，生產環境必須設置）
const STATE_SECRET = SERVICE_ROLE_KEY ? SERVICE_ROLE_KEY.substring(0, 32) : 'default-secret-key-for-development-only-32'
```

### 3. 在實際使用時驗證環境變數

**在 `handleCallback` 中添加驗證**:
```typescript
// ✅ 在實際使用時驗證環境變數
if (!SUPABASE_URL || !SERVICE_ROLE_KEY) {
  console.error('[handleCallback] Missing SUPABASE_URL or SERVICE_ROLE_KEY')
  const errorUrl = getErrorRedirectUrl('server_config_error', 'Server configuration error')
  if (req.method === 'POST') {
    return new Response(
      JSON.stringify({ redirectUrl: errorUrl }),
      {
        status: 200,
        headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      }
    )
  }
  return Response.redirect(errorUrl)
}
```

## 📋 測試步驟

1. **測試 OPTIONS 請求**:
   - 在 App 中點擊 LINE 登入按鈕
   - 檢查 Edge Function 日誌，確認 OPTIONS 請求返回 200（不再是 503）
   - 確認看到 `[line-auth] CORS preflight request handled immediately` 日誌

2. **測試 POST 請求**:
   - 確認 POST 請求能夠成功執行
   - 確認能夠獲取 `authUrl` 並重定向到 LINE 授權頁面

## ✅ 預期結果

- ✅ OPTIONS 請求返回 200 狀態碼和正確的 CORS headers（不再是 503）
- ✅ POST 請求成功執行
- ✅ Edge Function 能夠正常啟動
- ✅ 不再出現 CORS 錯誤

## 🔍 技術細節

### 為什麼 `!` 斷言會導致問題

1. **運行時行為**: `!` 斷言運算符不會在賦值時檢查值
2. **延遲錯誤**: 如果值為 `undefined`，錯誤會在實際使用時才發生
3. **Edge Function 啟動失敗**: 如果錯誤發生在模組載入時，會導致整個 Edge Function 無法啟動

### 為什麼使用 `|| ''` 更好

1. **安全默認值**: 提供空字符串作為默認值，避免運行時錯誤
2. **延遲驗證**: 在實際使用時才驗證環境變數，允許 Edge Function 正常啟動
3. **更好的錯誤處理**: 可以在實際使用時提供更友好的錯誤訊息

## 📝 注意事項

1. **環境變數設置**: 確保在 Supabase Dashboard 中正確設置所有環境變數
2. **生產環境**: 在生產環境中，所有環境變數都必須正確設置
3. **錯誤處理**: 確保所有使用環境變數的地方都有適當的錯誤處理

---

## ✅ 總結

**根本原因**: 使用 `!` 斷言運算符和模組載入時的錯誤導致 Edge Function 無法啟動。

**解決方案**: 移除 `!` 斷言，使用 `|| ''` 提供默認值，並在實際使用時驗證環境變數。

**結果**: Edge Function 現在應該能夠正常啟動，OPTIONS 請求應該返回 200 而不是 503。
