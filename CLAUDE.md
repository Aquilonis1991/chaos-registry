## Agent skills

### Issue tracker

Issues live in GitHub Issues (github.com/Aquilonis1991/chaos-registry), using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — one `CONTEXT.md` + `docs/adr/` at the repo root (neither exists yet; created lazily). See `docs/agents/domain.md`.
