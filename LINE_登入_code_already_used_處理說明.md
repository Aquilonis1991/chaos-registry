# LINE 登入 - code_already_used 錯誤處理說明

## ✅ 當前狀態

**修復已生效**：
- Edge Function 已正確處理重複請求
- 返回友好的錯誤訊息：`code_already_used`
- 前端已改進錯誤處理，顯示更友好的提示

---

## 🔍 問題分析

從日誌可以看到：
1. 用戶點擊 LINE 登入按鈕
2. 被重定向到 LINE 授權頁面
3. 授權完成後回調
4. 出現 `code_already_used` 錯誤

**可能的原因**：
- 多個回調請求同時到達 Edge Function
- Authorization code 只能使用一次
- 第一個請求成功後，後續請求會失敗

---

## ✅ 已實施的修復

### 1. Edge Function 錯誤處理

在 `line-auth` Edge Function 中：
- 檢測到 `invalid_grant` 錯誤時
- 返回友好的錯誤訊息：`code_already_used`
- 不再拋出技術異常

### 2. 前端錯誤處理改進

在 `OAuthCallbackHandler.tsx` 中：
- 特殊處理 `code_already_used` 錯誤
- 顯示友好的中文錯誤訊息
- 提示用戶重新嘗試登入

---

## 💡 用戶體驗

當出現 `code_already_used` 錯誤時：
- ✅ 顯示友好的錯誤訊息（不再是技術錯誤）
- ✅ 提示用戶重新嘗試登入
- ✅ 自動導航回登入頁面

---

## 🎯 建議的用戶操作

如果遇到 `code_already_used` 錯誤：

1. **重新嘗試登入**：
   - 點擊 LINE 登入按鈕
   - 完成授權流程
   - 應該可以成功登入

2. **如果問題持續**：
   - 等待幾秒鐘後再嘗試
   - 確認網路連接正常
   - 完全關閉 APP 後重新啟動

---

## 📋 技術細節

### Authorization Code 特性

- **只能使用一次**：OAuth 2.0 的 authorization code 設計為只能使用一次
- **短暫有效**：通常只有幾分鐘的有效期
- **一次性**：使用後立即失效

### 為什麼會出現 code_already_used？

1. **瀏覽器行為**：
   - 自動重試失敗的請求
   - 多個標籤頁同時處理回調

2. **網路問題**：
   - 請求超時導致重試
   - 網路不穩定導致重複發送

3. **用戶行為**：
   - 多次點擊按鈕
   - 快速刷新頁面

---

## 🔧 未來改進建議

如果需要完全防止重複請求：

1. **實施數據庫追蹤**：
   - 創建 `oauth_used_codes` 表
   - 在處理前檢查 code 是否已使用
   - 完全防止重複處理

2. **改進前端邏輯**：
   - 加強 `isProcessing` 邏輯
   - 添加請求去重
   - 添加超時處理

---

## 📝 測試建議

1. **正常登入測試**：
   - 點擊 LINE 登入按鈕
   - 完成授權流程
   - 確認登入成功

2. **錯誤處理測試**：
   - 如果出現 `code_already_used` 錯誤
   - 確認顯示友好的錯誤訊息
   - 確認可以重新嘗試登入

---

**更新日期**：2026-01-14  
**狀態**：修復已生效，錯誤處理已改進
