# UI 文字管理系統驗證清單

## 📋 驗證日期：2025-11-12

---

## ✅ 驗證項目

### 一、前端頁面驗證（18個）

#### 1. 用戶功能頁面
- [ ] **ProfilePage.tsx** - 個人資料頁面
  - [ ] 檢查所有文字是否使用 `getText()` 而非硬編碼
  - [ ] 測試語言切換（zh/en/ja）是否正常運作
  - [ ] 驗證所有按鈕、標籤、錯誤訊息是否正確顯示

- [ ] **TopicHistoryPage.tsx** - 主題發起紀錄
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證空狀態、狀態標籤、統計文字

- [ ] **NotificationsPage.tsx** - 通知與公告
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證分類標籤、通知類型

- [ ] **MissionPage.tsx** - 任務中心
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證任務名稱、進度條、按鈕文字

- [ ] **HomePage.tsx** - 首頁
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證標籤、篩選器、空狀態

- [ ] **CreateTopicPage.tsx** - 建立主題
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證表單標籤、驗證訊息、按鈕

- [ ] **ContactPage.tsx** - 聯絡我們
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證表單標籤、選項、按鈕

- [ ] **RechargePage.tsx** - 儲值頁面
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證金額選項、按鈕、說明文字

- [ ] **TokenUsageHistoryPage.tsx** - 代幣使用紀錄
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證表格標題、狀態、空狀態

- [ ] **VoteHistoryPage.tsx** - 投票紀錄
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證表格標題、狀態、空狀態

- [ ] **VoteDetailPage.tsx** - 投票詳情
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證按鈕、統計、選項文字

- [ ] **AuthPage.tsx** - 登入註冊
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證表單標籤、驗證訊息、按鈕

#### 2. 錯誤與條款頁面
- [ ] **NetworkErrorPage.tsx** - 網路錯誤頁面
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證錯誤訊息、按鈕、原因列表

- [ ] **ErrorPage.tsx** - 錯誤頁面
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證錯誤訊息、按鈕

- [ ] **NotFound.tsx** - 404 頁面
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證訊息、按鈕

- [ ] **TermsPage.tsx** - 服務條款頁面
  - [ ] 檢查標題是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 注意：條款內容本身保持原樣（可後續擴展）

- [ ] **PrivacyPage.tsx** - 隱私政策頁面
  - [ ] 檢查標題是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 注意：政策內容本身保持原樣（可後續擴展）

#### 3. 後台管理頁面
- [ ] **AdminPage.tsx** - 後台管理主頁
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證標籤、導航

---

### 二、前端組件驗證（12個）

#### 1. 導航組件
- [ ] **BottomNav.tsx** - 底部導航
  - [ ] 檢查所有導航項目是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：首頁、儲值、任務、個人

#### 2. 搜尋相關組件
- [ ] **SearchBar.tsx** - 搜尋欄
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換

- [ ] **SearchFilters.tsx** - 搜尋篩選
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換

- [ ] **SearchResults.tsx** - 搜尋結果
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換

#### 3. 公告組件
- [ ] **AnnouncementCarousel.tsx** - 公告輪播
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換

#### 4. 對話框組件
- [ ] **ReportDialog.tsx** - 檢舉對話框
  - [ ] 檢查所有檢舉類型、錯誤訊息、按鈕是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：9 種檢舉類型、表單標籤、驗證訊息

- [ ] **EditTopicDialog.tsx** - 編輯主題對話框
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：表單標籤、驗證訊息、規則說明、按鈕

- [ ] **DeleteTopicDialog.tsx** - 刪除主題對話框
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：警告訊息、確認文字、按鈕

- [ ] **ChangePasswordDialog.tsx** - 修改密碼對話框
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：表單標籤、驗證訊息、強度標籤、按鈕
  - [ ] 特別注意：密碼驗證 schema 使用動態 UI 文字

- [ ] **ErrorFeedback.tsx** - 錯誤回饋組件
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：表單標籤、錯誤訊息、成功訊息、按鈕
  - [ ] 驗證：支援自訂 triggerText，但預設使用 UI 文字管理

#### 5. 系統組件
- [ ] **AdBanner.tsx** - 廣告橫幅
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：佔位符文字、除錯訊息

- [ ] **ErrorBoundary.tsx** - 錯誤邊界
  - [ ] 檢查所有文字是否使用 `getText()`
  - [ ] 測試語言切換
  - [ ] 驗證：錯誤訊息、按鈕、提示文字
  - [ ] 驗證：類組件結構保持，但 UI 使用函數組件渲染

---

### 三、後台管理組件驗證（2個）

- [ ] **UITextManager.tsx** - UI文字管理
  - [ ] 檢查是否使用 `useUIText`
  - [ ] 注意：此組件本身就是 UI 文字管理系統

- [ ] **UserManager.tsx** - 用戶管理
  - [ ] 檢查是否使用 `useUIText`
  - [ ] 注意：後台組件不需要接入，但已接入的保持現狀

---

## 🔍 驗證步驟

### 步驟 1：代碼檢查
1. [ ] 確認所有頁面和組件已導入 `useLanguage` 和 `useUIText`
2. [ ] 確認所有硬編碼中文已替換為 `getText(key, fallback)`
3. [ ] 確認沒有遺漏的硬編碼文字（使用 grep 搜尋 `[\u4e00-\u9fa5]`）
4. [ ] 確認所有組件無 linter 錯誤

### 步驟 2：功能測試
1. [ ] 測試語言切換功能（zh → en → ja）
2. [ ] 驗證每個頁面的文字是否正確顯示
3. [ ] 驗證錯誤訊息是否正確顯示
4. [ ] 驗證表單驗證訊息是否正確顯示
5. [ ] 驗證按鈕文字是否正確顯示

### 步驟 3：UI 文字管理後台驗證
1. [ ] 登入後台管理系統
2. [ ] 進入「UI 文字管理」頁面
3. [ ] 檢查所有 key 是否已建立
4. [ ] 測試修改文字內容是否即時生效
5. [ ] 測試多語系切換是否正常

### 步驟 4：邊界情況測試
1. [ ] 測試當 UI 文字 key 不存在時，fallback 是否正確顯示
2. [ ] 測試語言切換時的載入狀態
3. [ ] 測試錯誤邊界組件的文字顯示
4. [ ] 測試網路錯誤頁面的文字顯示

---

## 📊 驗證統計

### 應驗證項目
- **前端頁面**：18 個
- **前端組件**：12 個
- **後台管理組件**：2 個
- **總計**：32 個

### 驗證重點
1. ✅ 所有硬編碼文字已替換
2. ✅ 所有組件支援多語系切換
3. ✅ 所有 fallback 文字正確
4. ✅ 無 linter 錯誤
5. ✅ UI 文字管理後台可正常管理

---

## 🎯 驗證檢查點

### 關鍵功能檢查
- [ ] **檢舉功能**：ReportDialog.tsx 的所有檢舉類型文字
- [ ] **編輯功能**：EditTopicDialog.tsx 的所有驗證訊息
- [ ] **刪除功能**：DeleteTopicDialog.tsx 的所有警告訊息
- [ ] **密碼修改**：ChangePasswordDialog.tsx 的所有驗證規則
- [ ] **錯誤回報**：ErrorFeedback.tsx 的所有表單文字
- [ ] **錯誤邊界**：ErrorBoundary.tsx 的所有錯誤訊息

### 語言切換測試
- [ ] 切換到英文（en），檢查所有文字是否正確顯示
- [ ] 切換到日文（ja），檢查所有文字是否正確顯示
- [ ] 切換回中文（zh），檢查所有文字是否正確顯示
- [ ] 驗證語言設定是否持久化保存

### 後台管理驗證
- [ ] 在後台修改 UI 文字內容
- [ ] 驗證前端是否即時更新
- [ ] 驗證多語系文字是否可分別管理
- [ ] 驗證新增 key 是否正確顯示

---

## 📝 驗證備註

1. **條款和政策內容**：
   - TermsPage.tsx 和 PrivacyPage.tsx 的完整內容尚未接入
   - 目前只處理了標題部分
   - 如需完整接入，需要大量 UI 文字 key

2. **後台管理組件**：
   - 根據要求，後台管理組件不需要接入 UI 文字管理系統
   - 但已接入的組件（UITextManager、UserManager）保持現狀

3. **ErrorBoundary 技術實現**：
   - 保持類組件結構以支援 React Error Boundary
   - 使用內部函數組件 `ErrorUI` 來使用 hooks
   - 這樣既保持了 Error Boundary 功能，又可以使用 UI 文字管理

4. **動態文字生成**：
   - ChangePasswordDialog.tsx 的密碼驗證 schema 使用動態 UI 文字
   - 這意味著驗證訊息會根據當前語言動態生成

---

## ✅ 驗證完成確認

- [ ] 所有頁面驗證完成
- [ ] 所有組件驗證完成
- [ ] 語言切換測試完成
- [ ] 後台管理驗證完成
- [ ] 邊界情況測試完成
- [ ] 無遺漏的硬編碼文字
- [ ] 所有功能正常運作

**驗證人員簽名**：________________  
**驗證日期**：________________  
**備註**：________________

