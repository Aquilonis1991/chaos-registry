# 任務達成條件驗證問題分析

## 🔍 發現的問題

### 問題 1：任務完成函數未驗證達成條件

**位置**：`supabase/functions/complete-mission/index.ts`

**問題描述**：
- `complete-mission` Edge Function 只檢查任務是否已經完成
- **沒有驗證用戶是否真的達成了任務條件**
- 例如：用戶可以在未投票的情況下完成「新手上路」任務

**當前邏輯**：
```typescript
// 只檢查是否已完成，不驗證達成條件
if (userMission && userMission.completed) {
  // 返回錯誤
}
// 直接標記為完成，不驗證條件
await supabaseClient.from('user_missions').update({ completed: true });
```

**風險**：
- 用戶可以通過直接調用 API 完成未達成的任務
- 前端驗證可以被繞過
- 可能導致獎勵被濫發

## ✅ 解決方案

### 方案 1：在 Edge Function 中添加條件驗證（推薦）

修改 `complete-mission` Edge Function，在完成任務前驗證達成條件：

```typescript
// 驗證任務達成條件
const { data: userStats } = await supabaseClient
  .rpc('get_user_stats', { p_user_id: user.id });

switch (mission_id) {
  case 'first_vote':
    if (userStats.totalVotes === 0) {
      return new Response(
        JSON.stringify({ error: '任務條件未達成：需要完成第一次投票' }),
        { status: 400, headers: corsHeaders }
      );
    }
    break;
  
  case 'vote_lover':
    const target = await getConfig('mission_vote_lover_target', 10);
    if (userStats.uniqueTopicVotes < target) {
      return new Response(
        JSON.stringify({ error: `任務條件未達成：需要對 ${target} 個不同主題投票` }),
        { status: 400, headers: corsHeaders }
      );
    }
    break;
  
  case 'topic_creator':
    if (userStats.topicsCreated === 0) {
      return new Response(
        JSON.stringify({ error: '任務條件未達成：需要發起一個主題' }),
        { status: 400, headers: corsHeaders }
      );
    }
    break;
  
  case 'login_7days':
    const loginStreak = await getLoginStreak(user.id);
    const streakTarget = await getConfig('mission_7days_login_target', 7);
    if (loginStreak < streakTarget) {
      return new Response(
        JSON.stringify({ error: `任務條件未達成：需要連續登入 ${streakTarget} 天` }),
        { status: 400, headers: corsHeaders }
      );
    }
    break;
}
```

### 方案 2：使用資料庫函數驗證（更安全）

創建一個資料庫函數來驗證任務條件：

```sql
CREATE OR REPLACE FUNCTION public.verify_mission_condition(
  p_user_id uuid,
  p_mission_id text
)
RETURNS boolean
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
DECLARE
  v_stats RECORD;
  v_streak INTEGER;
  v_target INTEGER;
BEGIN
  -- 獲取用戶統計
  SELECT * INTO v_stats FROM get_user_stats(p_user_id);
  
  CASE p_mission_id
    WHEN 'first_vote' THEN
      RETURN v_stats.total_votes > 0;
    
    WHEN 'vote_lover' THEN
      SELECT value::INTEGER INTO v_target 
      FROM system_config 
      WHERE key = 'mission_vote_lover_target';
      RETURN COALESCE(v_stats.unique_topic_votes, 0) >= COALESCE(v_target, 10);
    
    WHEN 'topic_creator' THEN
      RETURN v_stats.topics_created > 0;
    
    WHEN 'login_7days' THEN
      SELECT login_streak INTO v_streak FROM profiles WHERE id = p_user_id;
      SELECT value::INTEGER INTO v_target 
      FROM system_config 
      WHERE key = 'mission_7days_login_target';
      RETURN COALESCE(v_streak, 0) >= COALESCE(v_target, 7);
    
    ELSE
      RETURN true; -- 其他任務暫時不驗證
  END CASE;
END;
$$;
```

然後在 Edge Function 中調用：

```typescript
const { data: isValid, error: verifyError } = await supabaseClient
  .rpc('verify_mission_condition', {
    p_user_id: user.id,
    p_mission_id: mission_id
  });

if (!isValid) {
  return new Response(
    JSON.stringify({ error: '任務條件未達成' }),
    { status: 400, headers: corsHeaders }
  );
}
```

## 📋 需要驗證的任務條件

1. **新手上路** (`first_vote`)
   - 條件：`totalVotes > 0`
   - 配置：無（固定條件）

2. **投票愛好者** (`vote_lover`)
   - 條件：`uniqueTopicVotes >= mission_vote_lover_target`
   - 配置：`mission_vote_lover_target`（默認：10）

3. **話題創造者** (`topic_creator`)
   - 條件：`topicsCreated > 0`
   - 配置：無（固定條件）

4. **7天登入** (`login_7days`)
   - 條件：`loginStreak >= mission_7days_login_target`
   - 配置：`mission_7days_login_target`（默認：7）

5. **觀看廣告** (`watch_ad`)
   - 條件：已觀看完整廣告（由 watch-ad Edge Function 處理）
   - 配置：`mission_watch_ad_limit`（每日上限）

6. **每日登入** (`daily_login`)
   - 條件：當天未簽到（由 record_daily_login 函數處理）
   - 配置：`mission_daily_login_reward`（獎勵數量）

## 🎯 建議實施步驟

1. **立即修復**：在 Edge Function 中添加條件驗證
2. **長期優化**：創建資料庫驗證函數，提高安全性
3. **測試驗證**：確保所有任務條件驗證正確運作

## ⚠️ 注意事項

- 每日登入和觀看廣告任務已經有後端驗證，不需要額外驗證
- 其他任務需要在完成前驗證條件
- 驗證邏輯應該從 `system_config` 讀取目標值，確保與前端一致

