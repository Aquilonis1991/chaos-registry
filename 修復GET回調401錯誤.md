# 🔧 修復 GET 回調 401 錯誤

## 問題描述

LINE 回調的 GET 請求返回 401 錯誤，即使使用 `Deno.serve` 也無法繞過 Supabase 運行時的 JWT 驗證。

## 根本原因

Supabase Edge Function 運行時在我們的代碼執行**之前**就檢查了 JWT，導致：
- GET 請求（LINE 回調，沒有 Authorization header）→ 401 錯誤
- POST 請求（從前端調用，有 Authorization header）→ 200 成功

## 解決方案

### 方法 1：在 Supabase Dashboard 中禁用 JWT 驗證（推薦）

1. **登入 Supabase Dashboard**
   - 前往 https://supabase.com/dashboard
   - 選擇你的專案

2. **導航到 Edge Functions**
   - 點擊左側選單的 **Edge Functions**
   - 找到 `line-auth` 函數

3. **編輯函數配置**
   - 點擊 `line-auth` 函數
   - 找到 **Settings** 或 **Configuration** 選項
   - 找到 **Verify JWT** 選項
   - **關閉** Verify JWT（設置為 `false`）

4. **保存並重新部署**
   - 保存配置
   - 重新部署函數（如果需要）

### 方法 2：使用 config.toml（已實施）

已在 `supabase/config.toml` 中添加配置：

```toml
[functions.line-auth]
verify_jwt = false
```

**注意**：這個配置可能需要在 Supabase Dashboard 中同步，或者通過 Supabase CLI 部署。

### 方法 3：手動驗證需要 JWT 的路由

由於我們禁用了全局 JWT 驗證，我們需要在代碼中手動驗證需要 JWT 的路由（POST 請求）。

**已實施**：在 `line-auth/index.ts` 中添加了手動 JWT 驗證邏輯（可選）。

## 驗證修復

1. **重新測試 LINE 登入**
   - 點擊 LINE 登入按鈕
   - 完成 LINE 授權
   - 確認不再出現 401 錯誤

2. **檢查 Edge Function 日誌**
   - 前往 Supabase Dashboard → Edge Functions → line-auth → Logs
   - 確認 GET callback 請求返回 302（重定向）而不是 401

3. **確認登入流程正常**
   - 確認用戶能夠成功登入
   - 確認 session 正確建立

## 安全考慮

⚠️ **重要**：禁用 JWT 驗證會讓整個 `line-auth` 函數公開訪問。

**緩解措施**：
1. ✅ GET callback 請求只重定向到前端，不處理敏感數據
2. ✅ POST 請求仍然可以手動驗證 JWT（如果需要）
3. ✅ 使用 CORS 和 Origin 驗證來限制訪問
4. ✅ 使用 state 簽名來防止 CSRF 攻擊

## ✅ 替代方案：獨立的 Callback Edge Function（已實施）

由於即使禁用 JWT 驗證，`line-auth` 函數仍然可能受到 Supabase 運行時的其他驗證機制影響，我們創建了一個獨立的 Edge Function 專門處理 callback：

### 步驟 1：創建獨立的 Callback Function

✅ **已創建**: `supabase/functions/line-auth-callback/index.ts`

這個函數：
- 只處理 GET 請求（LINE 回調）
- 不進行任何業務邏輯處理
- 立即重定向到前端
- 避免觸發任何 JWT 驗證

### 步驟 2：更新 LINE_REDIRECT_URI 環境變數

✅ **已更新**: `line-auth/index.ts` 中的 `LINE_REDIRECT_URI`

現在指向：
```
https://epyykzxxglkjombvozhr.supabase.co/functions/v1/line-auth-callback
```

### 步驟 3：在 Supabase Dashboard 中配置

1. **登入 Supabase Dashboard**
   - 前往 https://supabase.com/dashboard
   - 選擇你的專案

2. **導航到 Edge Functions**
   - 點擊左側選單的 **Edge Functions**
   - 找到 `line-auth-callback` 函數（新創建的）

3. **禁用 JWT 驗證**
   - 點擊 `line-auth-callback` 函數
   - 找到 **Settings** 或 **Configuration**
   - 找到 **Verify JWT with legacy secret** 選項
   - **關閉** Verify JWT（設置為 `false`）

4. **更新 LINE Developers Console**
   - 前往 https://developers.line.biz/console/
   - 找到你的 LINE Login Channel
   - 更新 Callback URL 為：
     ```
     https://epyykzxxglkjombvozhr.supabase.co/functions/v1/line-auth-callback
     ```
   - **保存**

### 步驟 4：更新環境變數

在 Supabase Dashboard 中更新 `line-auth` Edge Function 的環境變數：

1. 前往 **Edge Functions** → **line-auth** → **Settings**
2. 找到 **Environment Variables**
3. 更新 `LINE_REDIRECT_URI` 為：
   ```
   https://epyykzxxglkjombvozhr.supabase.co/functions/v1/line-auth-callback
   ```
4. **保存**

### 步驟 5：重新部署

✅ **已部署**: `line-auth-callback` 函數已部署

如果修改了 `line-auth` 函數，請重新部署：
```bash
npx supabase functions deploy line-auth
```

## 驗證修復

1. **確認 LINE_REDIRECT_URI 已更新**
   - 檢查 Edge Function 環境變數
   - 確認指向 `line-auth-callback`

2. **確認 LINE Developers Console 配置**
   - Callback URL 指向新的 `line-auth-callback` 函數

3. **重新測試 LINE 登入**
   - 點擊 LINE 登入按鈕
   - 完成 LINE 授權
   - 確認不再出現 401 錯誤

4. **檢查 Edge Function 日誌**
   - 前往 Supabase Dashboard → Edge Functions → line-auth-callback → Logs
   - 確認 GET callback 請求返回 302（重定向）而不是 401

## 其他替代方案

如果上述方法仍然無效，可以考慮：

1. **讓 LINE 直接回調到前端**
   - 修改 LINE 配置，callback URL 指向前端 URL
   - 前端處理 callback，然後調用 Edge Function
   - ⚠️ 這可能會導致 WebView 攔截問題

## 當前狀態

### 方法 1（原方案）
- ✅ 已更新 `config.toml`
- ✅ 已更新代碼，添加手動 JWT 驗證邏輯（可選）
- ⚠️ 問題：即使 "Verify JWT" 已關閉，仍然返回 401

### 方法 2（新方案 - 推薦）✅ 已實施
- ✅ 已創建獨立的 `line-auth-callback` Edge Function
- ✅ 已更新 `LINE_REDIRECT_URI` 指向新函數
- ✅ 已部署新函數
- ⚠️ **需要在 Supabase Dashboard 中**：
  1. 為 `line-auth-callback` 函數禁用 JWT 驗證
  2. 更新 `line-auth` 函數的 `LINE_REDIRECT_URI` 環境變數
  3. 更新 LINE Developers Console 中的 Callback URL

## 下一步

1. **檢查 Supabase Dashboard**
   - 確認 `line-auth` 函數的 JWT 驗證已禁用
   - 如果沒有此選項，聯繫 Supabase 支援

2. **重新測試**
   - 測試 LINE 登入流程
   - 確認 GET callback 不再返回 401

3. **監控日誌**
   - 持續監控 Edge Function 日誌
   - 確認沒有其他問題

---

**最後更新**: 2025-01-27
**狀態**: 配置已更新，等待 Supabase Dashboard 確認
