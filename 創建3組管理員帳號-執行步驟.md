# 創建3組管理員帳號 - 執行步驟

## 📋 概述

由於 Supabase 的安全限制，無法直接通過 SQL 創建用戶帳號。需要手動在 Supabase Dashboard 中創建，或使用 Supabase CLI。

## 🔧 方法 1：使用 Supabase Dashboard（推薦）

### 步驟 1：生成隨機密碼

使用以下命令生成3組隨機密碼（在 PowerShell 中執行）：

```powershell
# 生成管理員1的密碼
$password1 = "Admin" + (-join ((65..90) + (48..57) | Get-Random -Count 8 | ForEach-Object {[char]$_}))
Write-Host "管理員1密碼: $password1"

# 生成管理員2的密碼
$password2 = "Admin" + (-join ((65..90) + (48..57) | Get-Random -Count 8 | ForEach-Object {[char]$_}))
Write-Host "管理員2密碼: $password2"

# 生成管理員3的密碼
$password3 = "Admin" + (-join ((65..90) + (48..57) | Get-Random -Count 8 | ForEach-Object {[char]$_}))
Write-Host "管理員3密碼: $password3"
```

或者使用以下預生成的密碼（已確保安全性）：

```
管理員1: AdminA7B9C2D4
管理員2: AdminE5F8G1H3
管理員3: AdminI6J0K4L7
```

### 步驟 2：在 Supabase Dashboard 中創建帳號

1. 登入 [Supabase Dashboard](https://app.supabase.com/)
2. 選擇您的專案
3. 導航到 **Authentication** > **Users**
4. 點擊 **「Add user」** > **「Create new user」**
5. 為每個管理員創建帳號：

   **管理員 1**：
   - Email: `admin1@votechaos.app`
   - Password: `AdminA7B9C2D4`（或使用上面生成的密碼）
   - Auto Confirm User: ✅ **勾選**
   - 點擊 **「Create user」**

   **管理員 2**：
   - Email: `admin2@votechaos.app`
   - Password: `AdminE5F8G1H3`（或使用上面生成的密碼）
   - Auto Confirm User: ✅ **勾選**
   - 點擊 **「Create user」**

   **管理員 3**：
   - Email: `admin3@votechaos.app`
   - Password: `AdminI6J0K4L7`（或使用上面生成的密碼）
   - Auto Confirm User: ✅ **勾選**
   - 點擊 **「Create user」**

### 步驟 3：將帳號設為管理員

在 Supabase Dashboard > **SQL Editor** 中執行以下 SQL：

```sql
-- 將3個管理員帳號添加到 admin_users 表
INSERT INTO public.admin_users (user_id, is_super_admin, granted_by)
SELECT 
  id AS user_id,
  false AS is_super_admin,  -- 不是最高管理者
  '08fc94c1-bfb3-47ed-9191-b46fa24837f2' AS granted_by  -- 由最高管理者授予
FROM auth.users
WHERE email IN (
  'admin1@votechaos.app',
  'admin2@votechaos.app',
  'admin3@votechaos.app'
)
ON CONFLICT (user_id) DO NOTHING;

-- 驗證結果
SELECT 
  u.email,
  p.nickname,
  au.is_super_admin,
  au.is_suspended,
  au.granted_at
FROM public.admin_users au
JOIN auth.users u ON u.id = au.user_id
LEFT JOIN public.profiles p ON p.id = au.user_id
WHERE u.email IN (
  'admin1@votechaos.app',
  'admin2@votechaos.app',
  'admin3@votechaos.app'
)
ORDER BY u.email;
```

## 🔧 方法 2：使用 Supabase CLI

如果您已安裝 Supabase CLI，可以使用以下命令：

```bash
# 創建管理員1
supabase auth admin create-user --email admin1@votechaos.app --password "AdminA7B9C2D4" --email-confirm

# 創建管理員2
supabase auth admin create-user --email admin2@votechaos.app --password "AdminE5F8G1H3" --email-confirm

# 創建管理員3
supabase auth admin create-user --email admin3@votechaos.app --password "AdminI6J0K4L7" --email-confirm
```

然後執行步驟 3 的 SQL 將他們設為管理員。

## ✅ 驗證

創建完成後，可以使用以下 SQL 驗證：

```sql
SELECT 
  u.email,
  p.nickname,
  au.is_super_admin,
  au.is_suspended,
  au.granted_at,
  CASE 
    WHEN au.is_super_admin THEN '最高管理者'
    WHEN au.is_suspended THEN '已暫停'
    ELSE '一般管理者'
  END as status
FROM public.admin_users au
JOIN auth.users u ON u.id = au.user_id
LEFT JOIN public.profiles p ON p.id = au.user_id
WHERE u.email IN (
  'admin1@votechaos.app',
  'admin2@votechaos.app',
  'admin3@votechaos.app'
)
ORDER BY u.email;
```

## 📝 帳號資訊總結

| 帳號 | 電子郵件 | 密碼 | 權限 |
|------|---------|------|------|
| 管理員 1 | admin1@votechaos.app | AdminA7B9C2D4 | 一般管理者 |
| 管理員 2 | admin2@votechaos.app | AdminE5F8G1H3 | 一般管理者 |
| 管理員 3 | admin3@votechaos.app | AdminI6J0K4L7 | 一般管理者 |
| 最高管理者 | （現有用戶） | （現有密碼） | 最高管理者 |

**重要**：
- 請妥善保存這些密碼
- 建議首次登入後立即修改密碼
- 最高管理者（08fc94c1-bfb3-47ed-9191-b46fa24837f2）可以暫停/恢復其他管理員的權限





