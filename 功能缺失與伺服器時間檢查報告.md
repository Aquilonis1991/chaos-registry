# 專案功能檢查與缺失分析報告

根據 `PROJECT_REPORT.md` (ChaosRegistry 專案詳細報告) 與您提出的兩個重點：「**儲值補單功能**」與「**所有計時方便功能皆以伺服器為主**」，我對當前的 codebase 進行了全面掃描與檢查，以下為詳細的檢查結果與分析。

---

## 一、 儲值補單功能 (In-App Purchase Recovery)

**檢查結果：✅ 存在且功能完整，無缺失。**

根據 `PROJECT_REPORT.md` 第 7 節與當前原始碼比對，儲值補單相關的架構均建置完整並掛載於生命週期中。

### 存在的關鍵程式碼與機制：
1. **`src/lib/purchaseRecovery.ts`**
   - 包含 `recoverAndroidPendingPurchasesOnLogin` 函式，用於處理登入後的補單邏輯。
   - 使用 `RECOVERY_RECEIPT_STORAGE_KEY` 在本地端持久化未完成的交易憑證。
2. **`src/components/PurchaseRecoveryToastGate.tsx`**
   - 此元件已正確實作，用於攔截未完成的內購紀錄，並在使用者進入應用時跳出提示/重新驗證。
3. **`App.tsx` 與 `AuthContext.tsx`**
   - `App.tsx` 已掛載 `<PurchaseRecoveryToastGate />` 元件。
   - `AuthContext.tsx` 在登入成功後，有明確呼叫 `schedulePurchaseRecoveryRetries()` 觸發補單重試機制。
4. **`usePurchase.ts`**
   - 負責監聽 `CdvPurchase.store`，處理 Approved/Verified 狀態並與 `verify-google-play-purchase` 或 `verify-app-store-purchase` Edge Functions 溝通。

**結論**：目前儲值補單功能保留完整，並未被移出或遺失。

---

## 二、 計時方便/判定功能「以伺服器時間為主」

**檢查結果：⚠️ 有缺失 / 存在前後端時間基準不一的漏洞。**

雖然「後端/資料庫層」(Supabase Edge Functions 與 PostgreSQL) 的確是透過伺服器的 `now()` 來確保安全性，預防了實質的竄改；但在**前端 UI 呈現與狀態判定上，目前高度依賴客戶端 (手機/瀏覽器) 的本地時間。**

### 發現的問題點：
我在前端程式碼 (`src/`) 掃描了 `new Date()` 與 `Date.now()` 的使用情況，發現有大量核心業務邏輯的**時間比對**是使用本地時間，這代表使用者若**修改手機時間**，可能會導致 UI 產生異常行為或提前解鎖某些畫面狀態。

具體缺失的場景包含：
1. **主題結束判定 (Topic Expiration)**
   - 在 `HomePage.tsx`、`VoteDetailPage.tsx`、`useAiClosingStatement.ts` 等多處，判定主題是否結束的寫法為：
     `isEnded = topic.status === 'ended' || new Date(topic.end_at || 0) <= new Date()`
   - **潛在問題**：如果使用者將手機時間往後調，前端會誤以為此主題已達 `end_at` 並將狀態強制變為 `isEnded`，進而觸發後續的結束邏輯或是介面改變。
2. **用戶解封判定 (User Restrictions)**
   - 在 `UserRestrictionManager.tsx` 或是其他限制檢查中：
     `new Date(restriction.expires_at) < new Date()`
   - **潛在問題**：修改手機時間可能讓使用者在前端介面上看似已「解除限制」，雖然發送 request 到後端仍會被 SQL 的 `now()` 擋下，但會造成極差的 UX (按鈕可按但跳出權限錯誤)。
3. **沒有 `useServerTime` 或是時間偏移 (Time Offset) 機制**
   - 專案缺少了一個中心化的「伺服器時間同步機制」。一個標準的做法應該是在 App 初始化時，與伺服器要一次時間，算出 `Offset (伺服器時間 - 本地時間)`，這之後的所有前端時間判定都應該是 `new Date(Date.now() + offset)`。

**結論**：若要求「**所有**計時方便功能皆以伺服器為主」，目前前端嚴重不合規。前端 UI 目前依賴本地時間 (`new Date()`) 處理狀態變更，這是一項倒退或缺失。

---

## 三、 其他重要機制的健康檢查

依照 `PROJECT_REPORT.md`，我也針對其他核心功能進行了抽查，狀態如下：

1. **廣告獎勵系統 (`watch-ad`)：✅ 完整**
   - `watch-ad` Edge function 與前端套件皆存在。頻率與上限等依舊由 `system_config` 中的參數控制。
2. **第三方登入：✅ 完整**
   - 前端包含 `AuthPage`，後端有 `line-auth`、`line-auth-callback` 以及稍早剛修復好的 `twitter-auth`，機制皆存在且正在使用。
3. **AI 代寫/結語能力：✅ 完整**
   - `ai-chaos-rewrite.ts` 與 `generate-ai-closing.ts` Edge Functions 皆在，對應前端掛載點如字數限制改寫機制也沒有丟失。
4. **防連點/樂觀更新 (Optimistic Updates)：✅ 完整**
   - 投票行為 (`cast-vote`, `cast-free-vote`) 在 `useVoteOperations.ts` 有正確的防連點與 UI 樂觀更新設計。

---

## 💡 建議修復方案 (針對伺服器時間缺失)

為了補齊「計時功能以伺服器為主」的約定，建議實作以下機制來取代前端散落的 `new Date()`：

1. 建立一個專門的 Hook (`useServerTime.ts`)。
2. 在 App 啟動時 (`App.tsx` 或 `AuthContext`) 取回後端時間 (可以透過一個超輕量的 Supabase RPC `get_server_time`)。
3. 計算出 `TimeOffset = ServerTimestamp - LocalTimestamp`，存在全域或 Zustand store 中。
4. 將所有前端的到期檢查，例如 `new Date(topic.end_at) <= new Date()`，統一替換成透過 `TimeOffset` 計算後的伺服器時間。

**請問您是否需要我立即為您建立這個 `useServerTime` 同步機制，並將前端的 `new Date()` 到期判定統一修復為依賴伺服器時間？**
