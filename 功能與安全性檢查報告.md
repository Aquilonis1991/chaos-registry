# 功能與安全性檢查報告

**檢查目的**：確認所有功能與安全性無虞後，進行 iOS 版本構建。  
**版本**：1.0.47  
**檢查日期**：依本檔案建立日為準。

---

## 一、功能檢查總覽

### 1.1 路由與頁面

| 路徑 | 頁面 | 狀態 |
|------|------|------|
| `/` | Index（入口／導向） | ✅ |
| `/auth` | AuthPage（登入／註冊、LINE 等） | ✅ |
| `/auth/callback` | OAuthCallbackPage | ✅ |
| `/auth/deep-link-redirect` | DeepLinkRedirectPage | ✅ |
| `/auth/verify-redirect` | VerifyRedirectPage | ✅ |
| `/home` | HomePage（熱門／最新／參與過、無限捲動、曝光樣式） | ✅ |
| `/search` | SearchPage | ✅ |
| `/vote/:id` | VoteDetailPage（免費／付費投票、曝光申請） | ✅ |
| `/create` | CreateTopicPage（發起主題、曝光方案、禁字檢查） | ✅ |
| `/recharge` | RechargePage（儲值、內購） | ✅ |
| `/mission` | MissionPage（觀看廣告、每日登入） | ✅ |
| `/profile` | ProfilePage | ✅ |
| `/history/votes`、`/history/topics`、`/history/token-usage` | 各歷史頁 | ✅ |
| `/contact`、`/notifications`、`/terms`、`/privacy`、`/about` | 靜態／設定頁 | ✅ |
| `/admin` | AdminPage（管理後台） | ✅ |
| `/error`、`/network-error`、`*`（NotFound） | 錯誤頁 | ✅ |

### 1.2 核心功能

| 功能 | 狀態 |
|------|------|
| 登入／註冊（Email、LINE、Deep Link、OAuth 回調） | ✅ |
| 匿名／正式帳號、ProtectedRoute 導向 | ✅ |
| 主題列表（熱門／最新／參與過）、無限捲動、載入阻擋 | ✅ |
| 主題卡片曝光樣式（普通／中等／高級）、新增主題曝光選項樣式對應 | ✅ |
| 主題詳情、免費／付費投票、曝光申請 | ✅ |
| 發起主題、曝光方案、禁字檢查（checkBannedWords） | ✅ |
| 失序值顯示、儲值（內購）、任務觀看廣告領取 | ✅ |
| 獎勵廣告、原生進階廣告、穿插與間隔由 system_config 控制 | ✅ |
| 管理後台（需 admin_users 權限）、Web 版阻擋非管理員 | ✅ |

### 1.3 建置狀態

| 項目 | 狀態 |
|------|------|
| npm run build | ✅ |
| npx cap sync android | ✅ |
| gradlew bundleRelease（AAB） | ✅ 使用 bundleRelease，輸出為正式 AAB |
| 版本號 | package.json 1.0.47，與 build.gradle 同步 |

---

## 二、安全性檢查

### 2.1 環境變數與密鑰

| 項目 | 結果 |
|------|------|
| 前端僅使用 `VITE_*` 環境變數 | ✅ 無 service_role 或私鑰暴露 |
| Supabase 連線使用 anon key | ✅ client 僅用 VITE_SUPABASE_URL、VITE_SUPABASE_PUBLISHABLE_KEY |
| 環境變數範本 | ✅ env.example.txt 僅為範例，無真實密鑰 |
| .gitignore | ✅ `*.local` 涵蓋 .env.local，避免誤提交 |

### 2.2 認證與授權

| 項目 | 結果 |
|------|------|
| 密碼強度 | ✅ validationSchemas 與 AuthPage：8–72 字元、大小寫+數字 |
| 登入／註冊前端驗證 | ✅ loginSchema、signupSchema（zod） |
| OAuth / setSession | ✅ 使用 Supabase 官方 API，token 由 Supabase 處理 |
| 管理後台 | ✅ ProtectedRoute + useAdmin：Web 版非管理員導向 WebAdminOnlyPage |

### 2.3 輸入驗證與內容安全

| 項目 | 結果 |
|------|------|
| 發起主題 | ✅ createTopicSchema（標題／描述／選項／標籤長度與枚舉） |
| 投票 | ✅ voteSchema（topic_id UUID、option、amount 範圍） |
| 個人資料 | ✅ profileUpdateSchema（暱稱、頭像長度） |
| 禁字 | ✅ checkBannedWords RPC 於發起主題（標題／標籤／描述）呼叫 |
| 資料庫寫入 | ✅ 使用 Supabase client：.rpc()、.from().insert/update，參數化無拼接 SQL |
| XSS | ✅ 無將使用者輸入寫入 dangerouslySetInnerHTML；chart.tsx 僅注入程式內建 THEMES；admob.ts innerHTML 為靜態測試 UI |

### 2.4 金流與購買驗證

| 項目 | 結果 |
|------|------|
| 內購驗證 | ✅ 由 Edge Functions（verify-google-play-purchase、verify-app-store-purchase）以 SERVICE_ROLE 與商店憑證於後端驗證 |
| 憑證存放 | ✅ Google Play / App Store 憑證經 Deno.env 讀取，不進前端 |

### 2.5 資料庫與 RLS

| 項目 | 結果 |
|------|------|
| RLS | ✅ 專案內多份 migration 具 RLS 與 POLICY 設定 |
| 敏感操作 | ✅ 管理員功能、token 操作等依後端 RPC 或 RLS 控管 |

### 2.6 其他

| 項目 | 結果 |
|------|------|
| UI 文案新增 | ✅ UITextManager 使用 upsert(onConflict: 'key')，避免重複 key 錯誤 |
| 錯誤處理 | ✅ 關鍵流程有 try/catch、toast 或錯誤頁，ErrorBoundary 存在 |

---

## 三、已知或可接受事項

1. **Lint**：部分 no-explicit-any、android/build 等可能仍被掃到，不影響建置與執行。  
2. **verify-google-play-purchase**：註解中提及 JWT 簽名為模擬實作，正式環境需以正確 Google Play API 與憑證完成。  
3. **AdMob**：admob.ts 內測試廣告 UI 使用固定字串 innerHTML，非使用者輸入，屬可接受。  
4. **主機名／URL**：部分 fallback 使用專案 Supabase URL，屬公開 anon 端點，可接受。

---

## 四、結論與建議

- **功能**：路由、主題、投票、發起主題、曝光、失序值、儲值、任務、廣告、個人與歷史、管理後台等均已具備且對應目前設計。  
- **安全性**：環境變數與密鑰未暴露於前端、認證與授權有保護、輸入有驗證與禁字、金流驗證在後端、RLS 已佈署、無明顯 XSS/注入風險。  
- **建議**：可進行 **iOS 版本構建**；上線前再次確認 Google Play / App Store 購買驗證在生產環境已使用正式憑證與實作。

---

*本報告依目前程式庫與設定撰寫，若日後變更功能或依賴，請重新檢查。*
