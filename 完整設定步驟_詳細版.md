# Apple JWT 自動更新 - 完整設定步驟（詳細版）

## 📋 前置準備

在開始之前，請準備以下資訊：

1. **Team ID**：從 Apple Developer Portal 右上角取得
   - 格式：`ABC123DEF4`（10 個字元）
   - 位置：Apple Developer Portal > 右上角點擊帳號名稱 > 查看 Team ID

2. **Key ID**：從 Apple Developer Portal Keys 頁面取得
   - 格式：`XYZ789GHI0`（10 個字元）
   - 位置：Apple Developer Portal > Certificates, Identifiers & Profiles > Keys > 您的 Key

3. **.p8 檔案內容**：下載的 `AuthKey_XXX.p8` 檔案
   - 打開檔案（使用文字編輯器）
   - 複製完整內容（包括 `-----BEGIN PRIVATE KEY-----` 和 `-----END PRIVATE KEY-----`）

---

## 步驟 1：設定 GitHub Secrets（10-15 分鐘）

### 1.1 前往 GitHub Repository

1. 打開瀏覽器，前往您的 GitHub Repository
   - 網址格式：`https://github.com/[您的帳號]/[Repository名稱]`
   - 例如：`https://github.com/username/votechaos-main`

### 1.2 導航到 Secrets 設定頁面

1. 在 Repository 頁面，點擊 **Settings**（設定）標籤
   - 位置：Repository 頂部導航欄的最右側

2. 在左側選單中，找到 **Secrets and variables**
   - 展開後，點擊 **Actions**

3. 您會看到 **Repository secrets** 頁面
   - 這裡可以新增和管理 Secrets

### 1.3 新增第一個 Secret：`APPLE_TEAM_ID`

1. 點擊右上角的 **New repository secret** 按鈕

2. 填寫資訊：
   - **Name**：輸入 `APPLE_TEAM_ID`
     - ⚠️ **重要**：名稱必須完全一致，區分大小寫
   
   - **Secret**：輸入您的 Team ID
     - 例如：`ABC123DEF4`
     - 只輸入 Team ID，不要包含其他文字或空格

3. 點擊 **Add secret** 按鈕

4. 確認已成功新增
   - 您應該會看到 `APPLE_TEAM_ID` 出現在列表中

### 1.4 新增第二個 Secret：`APPLE_KEY_ID`

1. 再次點擊 **New repository secret** 按鈕

2. 填寫資訊：
   - **Name**：輸入 `APPLE_KEY_ID`
     - ⚠️ **重要**：名稱必須完全一致，區分大小寫
   
   - **Secret**：輸入您的 Key ID
     - 例如：`XYZ789GHI0`
     - 只輸入 Key ID，不要包含其他文字或空格

3. 點擊 **Add secret** 按鈕

4. 確認已成功新增
   - 您應該會看到 `APPLE_KEY_ID` 出現在列表中

### 1.5 新增第三個 Secret：`APPLE_KEY_FILE`

1. 再次點擊 **New repository secret** 按鈕

2. 填寫資訊：
   - **Name**：輸入 `APPLE_KEY_FILE`
     - ⚠️ **重要**：名稱必須完全一致，區分大小寫
   
   - **Secret**：貼上 `.p8` 檔案的完整內容
     - 必須包含完整的內容，例如：
       ```
       -----BEGIN PRIVATE KEY-----
       MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQgxc06bXb/D64XE54t
       oFKc2+SK5b2zrp6TSCx+JKI1VSygCgYIKoZIzj0DAQehRANCAARzcBemx7f+5wWP
       pXNq8EQAjnXWMXI51o/Y85FlLVpVIf23ro1Qe+ofH63B8pxh3kTzNRVo7e8Fj5iZ
       xTlEx9GA
       -----END PRIVATE KEY-----
       ```
     - ⚠️ **重要**：
       - 必須包含 `-----BEGIN PRIVATE KEY-----`
       - 必須包含 `-----END PRIVATE KEY-----`
       - 必須包含中間的所有內容
       - 不要修改任何內容

3. 點擊 **Add secret** 按鈕

4. 確認已成功新增
   - 您應該會看到 `APPLE_KEY_FILE` 出現在列表中

### 1.6 確認所有 Secrets 已設定

完成後，您應該看到三個 Secrets：

- ✅ `APPLE_TEAM_ID`
- ✅ `APPLE_KEY_ID`
- ✅ `APPLE_KEY_FILE`

⚠️ **重要提醒**：
- Secrets 一旦建立，就無法查看內容（只能看到名稱）
- 如果需要修改，必須刪除後重新建立
- Secrets 不會顯示在日誌或程式碼中

---

## 步驟 2：提交檔案到 GitHub（5-10 分鐘）

### 2.1 檢查檔案是否已存在

確認以下檔案已存在：

- `.github/workflows/update-apple-jwt.yml`
- `scripts/update-apple-jwt.js`
- `scripts/generate-apple-jwt.js`

### 2.2 開啟終端機或命令提示字元

1. 在專案資料夾中，開啟終端機或命令提示字元
   - 位置：`C:\Users\USER\Documents\Mywork\votechaos-main`

2. 確認當前目錄：
   ```bash
   pwd
   # 或 Windows PowerShell
   Get-Location
   ```

### 2.3 檢查 Git 狀態

1. 檢查檔案狀態：
   ```bash
   git status
   ```

2. 確認以下檔案是否顯示為未提交：
   - `.github/workflows/update-apple-jwt.yml`
   - `scripts/update-apple-jwt.js`
   - `scripts/generate-apple-jwt.js`

### 2.4 新增檔案到 Git

1. 新增 GitHub Actions 工作流程檔案：
   ```bash
   git add .github/workflows/update-apple-jwt.yml
   ```

2. 新增腳本檔案：
   ```bash
   git add scripts/update-apple-jwt.js
   git add scripts/generate-apple-jwt.js
   ```

   或一次新增所有相關檔案：
   ```bash
   git add .github/workflows/update-apple-jwt.yml scripts/update-apple-jwt.js scripts/generate-apple-jwt.js
   ```

3. 確認檔案已加入暫存區：
   ```bash
   git status
   ```
   - 應該看到檔案顯示為綠色（已加入暫存區）

### 2.5 提交檔案

1. 建立提交：
   ```bash
   git commit -m "Add GitHub Actions for Apple JWT auto-update"
   ```

2. 確認提交成功：
   ```bash
   git log --oneline -1
   ```
   - 應該看到剛才的提交訊息

### 2.6 推送到 GitHub

1. 推送到遠端 Repository：
   ```bash
   git push
   ```

2. 如果遇到認證問題：
   - 可能需要輸入 GitHub 帳號和密碼
   - 或使用 Personal Access Token

3. 確認推送成功：
   - 前往 GitHub Repository 頁面
   - 確認檔案已出現在 Repository 中

---

## 步驟 3：測試 GitHub Actions（5-10 分鐘）

### 3.1 前往 GitHub Actions 頁面

1. 在 GitHub Repository 頁面，點擊 **Actions** 標籤
   - 位置：Repository 頂部導航欄

2. 在左側選單中，找到 **Update Apple JWT Token** 工作流程
   - 如果沒有看到，可能需要重新整理頁面

### 3.2 手動觸發工作流程

1. 點擊 **Update Apple JWT Token** 工作流程

2. 在右側，點擊 **Run workflow** 按鈕
   - 位置：工作流程頁面的右上角

3. 選擇分支：
   - 從下拉選單中選擇分支（通常是 `main` 或 `master`）

4. 點擊 **Run workflow** 按鈕確認

### 3.3 監控執行過程

1. 您會看到一個新的執行記錄出現
   - 標題：`Update Apple JWT Token`（手動觸發）

2. 點擊執行記錄查看詳細資訊

3. 觀察執行步驟：
   - ✅ Checkout repository
   - ✅ Setup Node.js
   - ✅ Install dependencies
   - ✅ Create secrets directory
   - ✅ Write Apple private key
   - ✅ Generate JWT Token
   - ✅ Read generated token
   - ✅ Create GitHub Issue
   - ✅ Cleanup

4. 等待執行完成（通常需要 1-2 分鐘）

### 3.4 檢查執行結果

1. **成功情況**：
   - 所有步驟都顯示綠色 ✓
   - 最後顯示 "All jobs have passed"

2. **失敗情況**：
   - 某個步驟顯示紅色 ✗
   - 點擊失敗的步驟查看錯誤訊息

### 3.5 常見錯誤處理

#### 錯誤 1：Secrets 未設定

**錯誤訊息**：`Error: Missing required secret: APPLE_TEAM_ID`

**解決方案**：
- 確認所有三個 Secrets 都已設定
- 確認 Secret 名稱完全正確（區分大小寫）

#### 錯誤 2：.p8 檔案格式不正確

**錯誤訊息**：`Error: Invalid private key`

**解決方案**：
- 確認 `.p8` 檔案內容包含 BEGIN 和 END 標記
- 確認沒有多餘的空格或換行

#### 錯誤 3：無法建立 GitHub Issue

**錯誤訊息**：`Error: Resource not accessible by integration`

**解決方案**：
- 確認 Repository 的 Issues 功能已啟用
- 前往 Repository Settings > General > Features
- 確認 **Issues** 已勾選

---

## 步驟 4：檢查 GitHub Issue（2-3 分鐘）

### 4.1 前往 Issues 頁面

1. 在 GitHub Repository 頁面，點擊 **Issues** 標籤
   - 位置：Repository 頂部導航欄

2. 找到新建立的 Issue：
   - 標題：`🔄 Apple JWT Token 需要更新 - [日期]`
   - 標籤：`automated`, `apple-jwt`, `supabase`

### 4.2 查看 JWT Token

1. 點擊 Issue 標題進入詳細頁面

2. 在 Issue 內容中，找到 **JWT Token** 區塊

3. 複製完整的 JWT Token：
   - 這是一個很長的字串（包含多個 `.`）
   - 例如：`eyJraWQiOiJBQkMxMjNERUY0IiwidHlwIjoiSldUIiwiYWxnIjoiRVMyNTYifQ...`
   - ⚠️ **重要**：必須複製完整的 Token，包括所有部分

---

## 步驟 5：更新 Supabase（5-10 分鐘）

### 5.1 登入 Supabase Dashboard

1. 打開瀏覽器，前往 [Supabase Dashboard](https://app.supabase.com/)

2. 使用您的帳號登入

3. 選擇專案：
   - 專案名稱：`epyykzxxglkjombvozhr`
   - 或根據您的實際專案名稱選擇

### 5.2 導航到 Apple Provider 設定

1. 在左側導航欄，點擊 **Authentication**
   - 位置：左側選單中

2. 點擊 **Providers** 標籤
   - 位置：Authentication 頁面頂部

3. 找到 **Apple** Provider
   - 在 Providers 列表中

4. 點擊 **Apple** 卡片上的開關按鈕，或點擊 **Configure** 按鈕
   - 這會打開 Apple Provider 的設定頁面

### 5.3 填入 JWT Token

1. 找到 **Secret Key (for OAuth)** 欄位

2. 清除舊的內容（如果有的話）

3. 貼上剛才從 GitHub Issue 複製的 JWT Token
   - ⚠️ **重要**：
     - 必須貼上完整的 JWT Token
     - 不要有多餘的空格或換行
     - 確認格式正確（很長的字串，包含多個 `.`）

4. 確認其他欄位已正確填寫：
   - **Client IDs**：`com.votechaos.app.services,com.votechaos.app`
   - **Allow users without an email**：已勾選
   - **Callback URL**：`https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback`

### 5.4 儲存設定

1. 檢查所有欄位是否正確

2. 點擊 **Save** 按鈕

3. 等待儲存完成（通常幾秒鐘）

4. 確認沒有錯誤訊息
   - 如果顯示錯誤，檢查 JWT Token 格式是否正確

---

## 步驟 6：測試 Apple 登入（3-5 分鐘）

### 6.1 前往應用程式登入頁面

1. 打開應用程式（Web 版或 App 版）

2. 前往登入頁面
   - Web 版：`https://chaos-registry.vercel.app/auth`
   - 或本地：`http://localhost:5173/auth`

### 6.2 測試 Apple 登入

1. 找到「使用 Apple 登入」按鈕

2. 點擊按鈕

3. 應該會跳轉到 Apple 登入頁面
   - 如果沒有跳轉，檢查瀏覽器控制台的錯誤訊息

4. 輸入 Apple ID 和密碼

5. 授權應用程式

6. 應該會重定向回應用程式

7. 確認用戶已成功登入

### 6.3 檢查登入狀態

1. 確認用戶資訊已正確顯示

2. 確認用戶已建立或更新

3. 檢查是否有錯誤訊息

---

## ✅ 完成檢查清單

設定完成後，請確認：

### GitHub Secrets
- [ ] `APPLE_TEAM_ID` 已設定
- [ ] `APPLE_KEY_ID` 已設定
- [ ] `APPLE_KEY_FILE` 已設定

### GitHub Repository
- [ ] 檔案已提交到 GitHub
- [ ] GitHub Actions 工作流程已存在

### GitHub Actions
- [ ] 測試執行成功
- [ ] 所有步驟都顯示綠色 ✓
- [ ] GitHub Issue 已建立

### Supabase
- [ ] JWT Token 已更新
- [ ] 所有欄位已正確填寫
- [ ] 設定已儲存

### 功能測試
- [ ] Apple 登入功能正常
- [ ] 用戶可以成功登入
- [ ] 沒有錯誤訊息

---

## 🎉 完成！

設定完成後，GitHub Actions 會：
- ✅ 每 150 天自動執行
- ✅ 自動建立 GitHub Issue 提醒您
- ✅ 完全自動化，無需手動操作

您只需要：
1. 定期檢查 GitHub Issue（每 150 天）
2. 複製 JWT Token
3. 更新到 Supabase

---

## 🆘 需要協助？

如果遇到問題，請告訴我：

1. **您目前在哪個步驟？**
   - 步驟 1：設定 GitHub Secrets
   - 步驟 2：提交檔案到 GitHub
   - 步驟 3：測試 GitHub Actions
   - 步驟 4：檢查 GitHub Issue
   - 步驟 5：更新 Supabase
   - 步驟 6：測試 Apple 登入

2. **遇到了什麼錯誤訊息？**
   - 請複製完整的錯誤訊息

3. **需要我檢查什麼？**
   - 我可以協助檢查檔案、驗證設定、解答問題

---

## 📚 相關文件

- `GitHub_Actions_Apple_JWT_設定指南.md` - 完整設定指南
- `GitHub_Actions_快速設定.md` - 快速參考
- `Apple_JWT_生成指南.md` - JWT 生成說明
