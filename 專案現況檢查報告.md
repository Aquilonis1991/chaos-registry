# VoteChaos 專案現況檢查報告

> **檢查日期**: 2025  
> **專案版本**: 1.0.38

---

## 一、專案架構概覽

| 項目 | 技術 / 狀態 |
|------|-------------|
| **前端** | React 18.3.1 + Vite 5.4.19 + TypeScript |
| **原生打包** | Capacitor 6.1.2（Android / iOS） |
| **後端** | Supabase（Auth、PostgreSQL、Edge Functions） |
| **路由** | React Router（BrowserRouter） |
| **UI** | Radix UI、Tailwind、shadcn/ui 風格組件 |

---

## 二、頁面與路由

| 路徑 | 頁面 | 保護 | 說明 |
|------|------|------|------|
| `/` | Index | - | 入口，依登入狀態導向 |
| `/auth` | AuthPage | - | 登入（含 LINE） |
| `/home` | HomePage | ProtectedRoute | 首頁、主題列表、無限滾動 |
| `/search` | SearchPage | ProtectedRoute | 搜尋 |
| `/vote/:id` | VoteDetailPage | ProtectedRoute | 主題詳情與投票 |
| `/create` | CreateTopicPage | requireAuth | 發起主題 |
| `/recharge` | RechargePage | requireAuth | 儲值（含內購） |
| `/mission` | MissionPage | requireAuth | 任務（含觀看廣告） |
| `/profile` | ProfilePage | requireAuth | 個人資料 |
| `/history/votes` | VoteHistoryPage | ProtectedRoute | 投票紀錄（含時間篩選） |
| `/history/topics` | TopicHistoryPage | ProtectedRoute | 主題發起紀錄（含時間篩選） |
| `/history/token-usage` | TokenUsageHistoryPage | ProtectedRoute | 代幣使用紀錄（含時間篩選） |
| `/admin` | AdminPage | requireAuth | 後台管理 |
| `/terms`, `/privacy`, `/about` | 靜態頁 | - | 條款、隱私、關於 |
| `/error`, `/network-error` | 錯誤頁 | - | 錯誤處理 |

**頁面總數**: 約 26 個（含 OAuth 回調、驗證、NotFound 等）。

---

## 三、近期已實作功能

### 1. 首頁無限滾動
- **位置**: `HomePage.tsx`、`useTopics.tsx`
- **分頁**: 熱門、最新、參與過 三個標籤皆支援
- **行為**: Intersection Observer 觸發載入更多；依 `currentTab`、`location.pathname` 正確綁定與清理
- **去重**: 同一分頁內依主題 ID 去重，避免重複卡片

### 2. 歷史紀錄時間篩選
- **位置**: `TimeFilter.tsx`、`VoteHistoryPage`、`TopicHistoryPage`、`TokenUsageHistoryPage`
- **選項**: 全部、1 天、1 週、1 個月、3 個月、6 個月、1 年
- **權限**: 非管理員僅能查 1 年內；1 年以前僅管理員可調取
- **文案**: 使用 UI 文字管理（`timeFilter.option.*`），需在 DB 執行 `時間篩選UI文字SQL插入語句.sql`

### 3. 廣告整合（見 `廣告類型詳細報告.md`）
- **獎勵廣告**: 已整合於任務頁，配合 `watch-ad` Edge Function
- **Banner**: 已整合於首頁，目前為除錯/佔位模式
- **插頁**: 代碼就緒，尚未掛到具體流程
- **原生卡片**: 已整合於首頁主題列表，使用 `native-ad-plugin`、`adInsertion.tsx`

### 4. 內購與代幣
- **Android**: `cordova-plugin-purchase`、`verify-google-play-purchase` Edge Function
- **代幣紀錄**: `token_transactions` 含 `metadata`（migration: `20260127_add_metadata_to_token_transactions.sql`）

### 5. LINE 登入
- **Edge Functions**: `line-auth`（取得授權 URL）、`line-auth-callback`（GET 回調）
- **前端**: Deep Link `votechaos://auth/callback`、`OAuthCallbackHandler`
- **已知修復**: 401（獨立 callback function）、503（環境變數與模組載入）、`code_already_used` 與重複回調處理
- **授權碼追蹤**: `line_auth_codes` 表與相關 migration（`20260115_add_line_auth_code_tracking.sql`）

---

## 四、後端與資料

### Edge Functions（supabase/functions）
- `line-auth`、`line-auth-callback`：LINE OAuth
- `watch-ad`：獎勵廣告後端
- `create-topic`、`cast-vote`、`cast-free-vote`：主題與投票
- `verify-google-play-purchase`、`verify-app-store-purchase`：內購驗證
- `get-system-config`：系統配置
- `complete-mission`、`initiate-purchase`
- AI 相關：`ai-chaos-rewrite`、`ai-topic-summary`、`ai-user-classification`
- `access-control`、`twitter-auth`

### Migrations（supabase/migrations）
- 約 47 個 migration 檔案
- 近期包含：LINE 授權碼追蹤、token_transactions metadata、admob 獎勵廣告單元、每日登入與任務相關修正、禁字與檢舉、主題摘要與不理性鑑定等

### 環境與連線
- **當前狀況總結.md** 曾提到專案 ID 與連線問題（`epyykzxxglkjombvozhr` vs `idfqzcsxvuxperxfieam`）與空白資料庫，屬環境/設定議題，需以實際 `.env` 與 Supabase 專案為準。

---

## 五、待確認／建議事項

| 項目 | 說明 |
|------|------|
| **Supabase 連線** | 確認 `.env.local` 的 `VITE_SUPABASE_URL`、`VITE_SUPABASE_PUBLISHABLE_KEY` 對應正確專案並重啟 dev |
| **DB 初始化** | 若為新環境，需在目標專案執行完整 schema（如 `00-完整資料庫架構.sql` 及必要 migrations） |
| **時間篩選文案** | 在 Supabase 執行 `時間篩選UI文字SQL插入語句.sql`，時間篩選多語系才會完整 |
| **LINE JWT** | 現況報告提到可能需在 Supabase Dashboard 關閉 Edge Function 的 JWT 驗證（視實際 401 是否仍發生） |
| **Banner 廣告** | 目前為除錯模式，上線前需改回真實 AdMob Banner |
| **插頁廣告** | 若需使用，需在適當流程（例如離開任務頁、完成投票）呼叫 `showInterstitialAd` |

---

## 六、檔案與模組對照（重點）

| 功能 | 主要檔案 |
|------|----------|
| 首頁與無限滾動 | `src/pages/HomePage.tsx`、`src/hooks/useTopics.tsx` |
| 時間篩選 | `src/components/TimeFilter.tsx` |
| 投票／主題／代幣紀錄 | `VoteHistoryPage`、`TopicHistoryPage`、`TokenUsageHistoryPage` + 對應 hooks |
| 廣告 | `src/lib/admob.ts`、`src/lib/adInsertion.tsx`、`NativeAdCard.tsx`、`AdBanner.tsx` |
| 內購 | `src/lib/purchase.ts`、`src/hooks/usePurchase.tsx` |
| LINE 登入 | `line-auth`、`line-auth-callback`、`OAuthCallbackHandler.tsx`、`AuthPage.tsx` |
| UI 文案 | `src/hooks/useUIText.tsx`、後台 UI 文字管理、`ui_texts` 表 |

---

## 七、專案健康度簡表

| 項目 | 狀態 |
|------|------|
| 核心頁面與路由 | ✅ 完整 |
| 首頁無限滾動（三標籤） | ✅ 已實作並修復返回後失效與去重 |
| 歷史紀錄時間篩選 | ✅ 已實作並接 UI 文字管理 |
| 廣告（獎勵／Banner／插頁／原生） | ✅ 代碼完成；Banner 為除錯、插頁未掛流程 |
| LINE 登入與回調 | ✅ 已多項修復；JWT 視實際環境確認 |
| 內購與代幣紀錄 | ✅ 已實作並有 metadata 支援 |
| 環境與 DB | ⚠️ 需依實際專案確認連線與 migrations |

---

**報告產生時間**: 依檢查當日  
**建議**: 先確認環境變數與 Supabase 專案、執行必要 migrations 與 UI 文字 SQL，再於真機/模擬器驗證首頁無限滾動與歷史時間篩選。
