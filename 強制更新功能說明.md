# 強制更新功能說明

## 功能概述

當後端設定「最低 App 版本」後，**僅在原生 App**（Capacitor Android / iOS）啟動時會檢查當前版本；若當前版本低於設定值，會顯示全螢幕「請更新至最新版本」畫面，並提供按鈕前往商店更新，**無法略過**。

- **網頁版**：不檢查、不阻擋。
- **未設定最低版本**：不阻擋，正常使用。

---

## 後端設定（system_config）

在 **管理後台 → 系統設定** 或直接在 `system_config` 表新增／編輯以下 key：

| Key | 說明 | 範例 |
|-----|------|------|
| `app_min_version_android` | Android 最低版本號（x.y.z）。**留空或不存在 = 不強制更新** | `1.0.48` |
| `app_min_version_ios` | iOS 最低版本號（x.y.z）。**留空或不存在 = 不強制更新** | `1.0.48` |
| `app_store_url_android` | Android 商店連結（選填） | `https://play.google.com/store/apps/details?id=com.votechaos.app` |
| `app_store_url_ios` | iOS App Store 連結（選填） | `https://apps.apple.com/app/id1234567890` |

- 未填 `app_store_url_android` / `app_store_url_ios` 時，Android 會使用預設 Google Play 連結（`com.votechaos.app`），iOS 需上架後在程式或後台改為實際 App Store 連結。

---

## 版本比較規則

- 格式：`x.y.z`（如 `1.0.47`），可含前綴 `v`（如 `v1.0.47`）。
- 比較方式：由左到右比較數字，例如 `1.0.48` > `1.0.47`，因此 1.0.47 會觸發強制更新。

---

## 流程說明

1. 使用者開啟 **原生 App**。
2. App 載入後從 `system_config` 讀取 `app_min_version_android` / `app_min_version_ios`（依平台，與其他設定一併快取）。
3. 取得當前 App 版本（Capacitor `App.getInfo().version` / `__APP_VERSION__`）。
4. 若該平台最低版本有值且 **當前版本 < 最低版本** → 顯示強制更新畫面，其餘內容不渲染。
5. 使用者點「前往更新」→ 以 Browser 外開或 `window.open` 開啟對應商店連結。

---

## 相關檔案

- **版本比較**：`src/lib/versionCheck.ts`
- **Hook**：`src/hooks/useForceUpdate.tsx`
- **畫面**：`src/components/ForceUpdateGate.tsx`
- **掛載**：`src/App.tsx`（包在 `ForceUpdateGate` 內）
- **Migration**：`supabase/migrations/20260201000000_add_app_min_version_config.sql`

---

## 啟用強制更新步驟

1. 執行 migration（若尚未執行）：  
   `supabase db push` 或於 Supabase Dashboard 執行該 SQL。
2. 在管理後台或 DB 將 `app_min_version_android` / `app_min_version_ios` 設為要強制的最低版本，例如 `1.0.48`。
3. 發布新版本 App（如 1.0.48）至商店。
4. 之後所有 1.0.47 及以下版本的使用者開啟 App 時會看到強制更新畫面，直至更新為止。

---

## 如何設定「強制更新的最低版本」

**意思**：當使用者 App 版本低於你設定的數字時，會強制顯示「請更新」畫面，無法進入 App。

### 方法一：Supabase Dashboard（建議）

1. 登入 [Supabase](https://supabase.com/dashboard) → 選專案 → **Table Editor**。
2. 打開 **system_config** 表。
3. 找到 key 為 **app_min_version_android** 或 **app_min_version_ios** 的那一列（依平台；若沒有則新增一列）：
   - **key**：`app_min_version_android` 或 `app_min_version_ios`
   - **value**：填 **最低版本號**，格式為 JSON 字串，例如 `"1.0.49"`（有雙引號）。
   - **category**：`app`
   - **description**：選填，如「最低 App 版本號」
4. 儲存。

**注意**：`value` 是 JSONB，要存成字串就必須有雙引號。正確範例：`"1.0.49"`。錯誤範例：`1.0.49`（數字會導致前端比對異常）。

### 方法二：SQL

在 Supabase **SQL Editor** 執行：

```sql
-- 設定最低版本為 1.0.49（低於此版本的 App 會強制更新；依平台執行對應 UPDATE）
UPDATE public.system_config SET value = '"1.0.49"'::jsonb WHERE key = 'app_min_version_android';
UPDATE public.system_config SET value = '"1.0.49"'::jsonb WHERE key = 'app_min_version_ios';
```

要**取消強制更新**時，把 value 改回空字串即可：

```sql
UPDATE public.system_config SET value = '""'::jsonb WHERE key = 'app_min_version_android';
UPDATE public.system_config SET value = '""'::jsonb WHERE key = 'app_min_version_ios';
```

### 建議流程

1. 在專案裡把 **package.json** 的 `version` 改為新版本（如 `1.0.49`）。
2. 重新打包並上架商店（見專案建置指南）。
3. **包裝完成後**，在專案根目錄執行：`npm run sql:force-update`，會依目前 `package.json` 版本輸出可貼到 **Supabase SQL Editor** 的 SQL。
4. 上架審核通過後，將該 SQL 貼到 Supabase SQL Editor 執行，即可將 **app_min_version_android** / **app_min_version_ios** 設為該版本。
5. 之後舊版 App 開啟時就會被要求更新。
