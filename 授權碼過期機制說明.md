# LINE 授權碼過期機制說明

## 過期機制

### 1. 數據庫記錄過期（10 分鐘）

- **`expires_at` 欄位**：每個授權碼記錄都有 `expires_at` 欄位，設置為創建時間 + 10 分鐘
- **檢查邏輯**：`is_line_auth_code_used()` 函數會檢查 `expires_at > now()`
- **過期後的行為**：
  - 過期後，該授權碼在數據庫中**不再被視為"已使用"**
  - 但實際上，LINE 的授權碼本身也已經過期（通常只有幾分鐘有效期）
  - 所以即使數據庫記錄過期，LINE 也不會接受這個授權碼

### 2. LINE 授權碼本身的過期

- LINE 的授權碼本身也有有效期（通常是 **5-10 分鐘**）
- 即使數據庫記錄還沒過期，LINE 的授權碼也可能已經失效
- 這是 OAuth 2.0 的標準安全機制

## 過期後的處理

### 自動清理機制

1. **觸發器清理**（已實現）：
   - 當插入新記錄時，如果過期記錄超過 1000 條，會自動清理
   - 這可以防止數據庫記錄無限增長

2. **手動清理**（可選）：
   ```sql
   SELECT public.cleanup_expired_line_auth_codes();
   ```

3. **定時清理**（建議）：
   - 可以使用 Supabase 的 **pg_cron** 擴展設置定時任務
   - 例如：每天凌晨清理一次過期記錄

### 設置定時清理任務（可選）

如果您的 Supabase 專案啟用了 `pg_cron` 擴展，可以設置定時任務：

```sql
-- 啟用 pg_cron 擴展（如果尚未啟用）
CREATE EXTENSION IF NOT EXISTS pg_cron;

-- 設置每天凌晨 2 點清理過期記錄
SELECT cron.schedule(
  'cleanup-expired-line-auth-codes',
  '0 2 * * *',  -- 每天凌晨 2 點（UTC）
  $$SELECT public.cleanup_expired_line_auth_codes()$$
);
```

## 過期記錄的影響

### 對系統的影響

1. **存儲空間**：
   - 過期記錄會佔用數據庫空間
   - 建議定期清理（通過觸發器或定時任務）

2. **查詢性能**：
   - 過期記錄不會影響查詢性能（因為有索引）
   - `is_line_auth_code_used()` 函數會自動過濾過期記錄

3. **安全性**：
   - 過期記錄不會影響安全性
   - 因為 `is_line_auth_code_used()` 只檢查未過期的記錄

## 實際場景

### 場景 1：正常登入流程

1. 用戶點擊 LINE 登入按鈕
2. 收到授權碼 `ABC123`
3. Edge Function 檢查：`is_line_auth_code_used('ABC123')` → `false`（未使用）
4. 處理授權碼，交換 token，建立 session
5. 標記為已使用：`mark_line_auth_code_used('ABC123', ...)`
6. 10 分鐘後，記錄過期（但已經成功登入，不影響）

### 場景 2：重複請求（防止重複處理）

1. 用戶點擊 LINE 登入按鈕
2. 收到授權碼 `ABC123`
3. 第一次請求：檢查 → 未使用 → 處理 → 標記為已使用
4. 第二次請求（可能是重複或重試）：
   - 檢查：`is_line_auth_code_used('ABC123')` → `true`（已使用）
   - **直接返回錯誤**，不進行任何處理
   - 避免 `code_already_used` 錯誤

### 場景 3：記錄過期後

1. 授權碼 `ABC123` 在 10 分鐘前被使用
2. 10 分鐘後，`expires_at < now()`，記錄過期
3. 如果有人嘗試使用過期的授權碼：
   - 檢查：`is_line_auth_code_used('ABC123')` → `false`（記錄已過期）
   - 但 LINE 的授權碼本身也已經過期
   - LINE 會返回 `invalid_grant` 錯誤
   - Edge Function 會正常處理這個錯誤

## 建議

1. **定期清理**：
   - 使用觸發器自動清理（已實現）
   - 或設置定時任務每天清理一次

2. **監控記錄數量**：
   ```sql
   -- 查看當前記錄數量
   SELECT COUNT(*) FROM public.line_auth_codes;
   
   -- 查看過期記錄數量
   SELECT COUNT(*) FROM public.line_auth_codes WHERE expires_at < now();
   ```

3. **調整過期時間**（如果需要）：
   - 當前設置為 10 分鐘
   - 可以根據 LINE 授權碼的實際有效期調整
   - 建議設置為 LINE 授權碼有效期的 2 倍（確保覆蓋）

## 總結

- ✅ **過期後**：數據庫記錄不再被視為"已使用"，但 LINE 授權碼本身也已經失效
- ✅ **自動清理**：通過觸發器自動清理過期記錄（當記錄數超過 1000 條時）
- ✅ **不影響功能**：過期記錄不會影響系統功能或安全性
- ✅ **可選優化**：可以設置定時任務定期清理
