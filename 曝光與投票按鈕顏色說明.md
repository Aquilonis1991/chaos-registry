# 曝光與投票按鈕顏色說明

## 1. 發起主題頁面 — 各曝光等級選取後的邊框與背景

| 等級 | 選取時邊框 | 選取時背景 |
|------|------------|------------|
| **普通** | 無邊（`border-0`） | 接近白的灰色：`bg-gradient-to-br from-gray-100 to-transparent`（深色模式 `dark:from-gray-800/50 dark:to-transparent`） |
| **中等** | 黃邊：`border-yellow-500/50` | 淡黃白漸層：`bg-gradient-to-br from-yellow-500/10 via-yellow-400/5 to-transparent`，陰影 `shadow-md shadow-yellow-500/10` |
| **高度** | 橘邊：`border-amber-500/60` | 淡黃紫漸層：`bg-gradient-to-br from-amber-500/15 via-purple-500/10 to-transparent`，陰影 `shadow-lg shadow-amber-500/20` |

未選取時：普通為 `border border-border/50`；中等／高度為 `border-border/50`。

---

## 2. 主題詳細頁 — 付費／免費投票按鈕是否符合規格

### 規格

- **vote 樣式（藍灰）**  
  - 背景：`bg-gradient-primary` → hsl(220 26% 33%) 到 hsl(220 26% 40%)  
  - 文字：`primary-foreground`（淺色）  
  - 邊框：`border-primary/20`

- **accent 樣式（暖黃橙）**  
  - 背景：`bg-gradient-accent` → hsl(43 76% 54%) 到 hsl(43 80% 60%)  
  - 文字：`accent-foreground`（深藍灰）  
  - 邊框：`border-accent/20`

### 實際使用（`VoteDetailPage` + `Button` variant）

| 按鈕 | variant | 樣式 | 是否符合 |
|------|---------|------|----------|
| 免費投票 | default + `className` 覆寫 | `bg-gradient-accent`、`text-accent-foreground`、`border border-accent/20` | ✅ 符合 accent |
| +1、+10 | `vote` | `bg-gradient-primary`、`text-primary-foreground`、`border-primary/20`（見 `button.tsx`） | ✅ 符合 vote |
| +100 及以上 | `accent` | `bg-gradient-accent`、`text-accent-foreground`、`border-accent/20` | ✅ 符合 accent |
| 自訂票數・投票 | `vote` | 同上 vote | ✅ 符合 vote |

**總結**：小額（<100）與自訂投票按鈕為藍灰（vote）；100 以上與免費投票按鈕為暖黃橙（accent）。免費投票按鈕已補上 `text-accent-foreground` 與 `border border-accent/20`，與規格一致。
