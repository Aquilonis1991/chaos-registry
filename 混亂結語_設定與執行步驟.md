# 混亂結語：你還需要執行的步驟（詳細）

混亂結語改為「主題結束後由後端排程自發生成」，前端只讀取顯示。

**設計行為：系統每小時檢查「未有結語」的已結束主題，並自動補上結語。**  
排程每小時整點執行一次，會找出「已結束且尚無 `topic_ai_summary`」的主題，逐一呼叫生成並寫入結語；前端詳情頁只會在有結語時顯示該區塊。

---

## 詳細要做的流程（從頭到尾）

依序做下面步驟，打勾表示已完成。

### 第一階段：Supabase 與部署（你已完成 ✅）

| # | 要做的事 | 狀態 |
|---|----------|------|
| 1 | 在 Supabase **Project Settings** → **Edge Functions** → **Secrets** 新增 **CRON_SECRET**，Value 用 `secrets/cron_secret.txt` 裡那一行 | ✅ |
| 2 | 本機執行：`npx supabase functions deploy generate-ai-closing` | ✅ |
| 3 | 本機執行：`npx supabase functions deploy process-ended-topics-closing` | ✅ |
| 4 | 到 Dashboard → **Edge Functions** 確認兩個 Function 都是已部署 | ✅ |

### 第二階段：設定排程（二選一，擇一完成）

**選方案 A：Supabase 內建 Cron（pg_cron + pg_net）**

| # | 要做的事 | 狀態 |
|---|----------|------|
| A1 | 到 **Database** → **Extensions**，啟用 **pg_cron**、**pg_net** | ☐ |
| A2 | 到 **SQL Editor**，執行兩行（值換成你的）：<br>`select vault.create_secret('你的_ANON_KEY', 'supabase_anon_key');`<br>`select vault.create_secret('你的_CRON_SECRET', 'cron_secret_closing');`<br>anon key = Project Settings → API → anon public；CRON = `secrets/cron_secret.txt` | ☐ |
| A3 | 在 **SQL Editor** 開啟並執行 **`sql_patches/schedule_process_ended_topics_closing.sql`** | ☐ |
| A4 | 到 **Database** → **Cron Jobs**（或查 `cron.job`）確認有 `process-ended-topics-closing` | ☐ |

**選方案 B：外部排程（cron-job.org）**

| # | 要做的事 | 狀態 |
|---|----------|------|
| B1 | 註冊/登入 [cron-job.org](https://cron-job.org)，點 **Create cron job** | ☐ |
| B2 | **URL** 填：`https://epyykzxxglkjombvozhr.supabase.co/functions/v1/process-ended-topics-closing` | ☐ |
| B3 | **Request Method** 選 **POST** | ☐ |
| B4 | **Request Headers** 新增兩筆：<br>① Name=`Authorization`，Value=`Bearer 你的_ANON_KEY`（anon 從 Project Settings → API → anon public 複製）<br>② Name=`x-cron-secret`，Value=`secrets/cron_secret.txt` 裡那一行 | ☐ |
| B5 | **Schedule** 選每小時或每天兩次，儲存並啟用 | ☐ |

### 第三階段：驗證

| # | 要做的事 | 狀態 |
|---|----------|------|
| 5 | 手動觸發一次：PowerShell 執行（替換成你的 anon key 與 CRON_SECRET）：<br>`curl.exe -X POST "https://epyykzxxglkjombvozhr.supabase.co/functions/v1/process-ended-topics-closing" -H "Authorization: Bearer 你的_ANON_KEY" -H "x-cron-secret: 你的CRON_SECRET" -H "Content-Type: application/json"`<br>回應應為 JSON，例如 `{"processed":0,"success":0,"failed":0,"results":[]}` | ☐ |
| 6 | 在 App/網頁找一個**已結束、尚未有結語**的主題，進詳情應看到「結語尚未生成…」 | ☐ |
| 7 | 等排程跑完（或再手動觸發一次），重新進入該主題詳情，應看到混亂結語出現 | ☐ |
| 8 | （可選）到 **Table Editor** 看 `topic_ai_summary` 是否有新列，`topics.ai_summary_generated` 是否變為 true | ☐ |

---

### 已完成進度（摘要）

| 步驟 | 狀態 |
|------|------|
| 一、Supabase 設定 CRON_SECRET | ✅ 已完成 |
| 二、部署 Edge Functions（generate-ai-closing、process-ended-topics-closing） | ✅ 已完成 |
| 三、設定排程（方案 A 或 方案 B） | ☐ **請依上方流程完成** |

---

## 一、在 Supabase 設定 CRON_SECRET

排程呼叫 Edge Function 時必須帶一組密碼（`x-cron-secret`），與專案裡的 `CRON_SECRET` 一致，避免被外人亂呼叫。

### 步驟

1. 登入 [Supabase Dashboard](https://supabase.com/dashboard)。
2. 選擇你的專案（例如 `epyykzxxglkjombvozhr`）。
3. 左側選 **Project Settings**（專案設定）。
4. 左側再選 **Edge Functions**。
5. 在 **Secrets** 區塊：
   - 若已有 `CRON_SECRET`：可沿用。
   - 若沒有：點 **Add new secret**：
     - **Name**：`CRON_SECRET`
     - **Value**：請複製專案內 **`secrets/cron_secret.txt`** 檔案中的那一行密碼（該資料夾已在 .gitignore，不會被提交）。
6. 儲存。

> **已生成的密碼記錄位置**：`secrets/cron_secret.txt`（32 字元，僅供本機使用；排程設定時請用同一組值填在 Header `x-cron-secret`）。

> 此 Secret 會給 **generate-ai-closing** 與 **process-ended-topics-closing** 兩個 Function 使用，不需分別設定兩次。

---

## 二、部署 Edge Functions

本機需已安裝 Supabase CLI 並登入（`npx supabase login`）。在專案根目錄（`votechaos-main`）執行。

### 2.1 部署「單一主題生成結語」（已支援排程呼叫）

```bash
cd c:\Users\USER\Documents\Mywork\votechaos-main
npx supabase functions deploy generate-ai-closing
```

- 若提示選專案，選你正在用的那個。
- 部署完成後，此 Function 仍可被**已登入用戶**呼叫（目前前端已不呼叫），且可被**帶正確 x-cron-secret 的排程**呼叫。

### 2.2 部署「批次處理已結束主題」的排程用 Function

```bash
npx supabase functions deploy process-ended-topics-closing
```

- 此為**新增**的 Function，專門給排程用。
- 部署完成後，你的 Function URL 會是：  
  `https://<你的專案-ref>.supabase.co/functions/v1/process-ended-topics-closing`  
  （專案 ref 在 Dashboard 網址或 Project Settings → General 可看到，例如 `epyykzxxglkjombvozhr`。）

### 2.3 確認

- Dashboard → **Edge Functions**：應看到 `generate-ai-closing` 與 `process-ended-topics-closing`，且為已部署狀態。

---

## 三、設定排程（二選一）

排程要**定期**對 `process-ended-topics-closing` 發送 **POST**，並帶 **x-cron-secret**，才會在主題結束後自發生成結語。

### 方案 A：使用 Supabase 內建 Cron（pg_cron + pg_net）

用資料庫的 **pg_cron** 排程 + **pg_net** 發 HTTP 請求，定期呼叫 `process-ended-topics-closing`。密碼放在 **Vault**，不寫進 SQL 檔。

#### 步驟 1：啟用擴展

1. 登入 Supabase Dashboard → 選你的專案。
2. 左側 **Database** → **Extensions**。
3. 搜尋並啟用：
   - **pg_cron**
   - **pg_net**  
   （若已啟用可略過。）

#### 步驟 2：把密碼寫入 Vault

在 **SQL Editor** 執行下面兩行（**請替換成你的實際值**，不要提交到 Git）：

```sql
-- 將 anon key 與 CRON_SECRET 寫入 Vault（各執行一次，值請替換）
select vault.create_secret('你的_ANON_KEY', 'supabase_anon_key');
select vault.create_secret('你的_CRON_SECRET', 'cron_secret_closing');
```

- **你的_ANON_KEY**：Supabase Dashboard → **Project Settings** → **API** → **Project API keys** → **anon public** 整段複製。
- **你的_CRON_SECRET**：`secrets/cron_secret.txt` 裡的那一行（與 Edge Functions 的 CRON_SECRET 相同）。

若 Vault 已存在同名 secret，可先刪除再建：  
`select vault.delete_secret(secret_id) from vault.secrets where name = 'supabase_anon_key';`（以及 `cron_secret_closing`），再重新 `create_secret`。

#### 步驟 3：建立排程

在 **SQL Editor** 開啟並執行專案內的：

**`sql_patches/schedule_process_ended_topics_closing.sql`**

- 會建立一筆 **pg_cron** 排程，名稱：`process-ended-topics-closing`。
- 預設為 **每小時整點**（`0 * * * *`）對 `process-ended-topics-closing` 發 POST，Header 從 Vault 讀取 `supabase_anon_key`、`cron_secret_closing`。
- 若要改為「每天 00:00 與 12:00」，把該檔最後一段的 `'0 * * * *'` 改成 `'0 0,12 * * *'` 後再執行。

#### 步驟 4：確認排程

- Dashboard → **Database** → **Cron Jobs**（若有此區塊）：應能看到 `process-ended-topics-closing`。
- 或執行查詢：`select * from cron.job where jobname = 'process-ended-topics-closing';`

（若專案沒有 pg_cron / pg_net 或不想用 Vault，可改用方案 B。）

### 方案 B：使用外部排程服務（推薦，步驟最明確）

用 [cron-job.org](https://cron-job.org)（免費）或 [EasyCron](https://www.easycron.com)、GitHub Actions 等，定期發 POST。

以 **cron-job.org** 為例（請依序完成）：

1. 註冊 / 登入 [cron-job.org](https://cron-job.org)。
2. 左側或上方點 **Create cron job**（或「新增 Cron Job」）。
3. 填寫：
   - **Title**：`Chaos 結語排程`（或自訂）。
   - **URL**（必填）：  
     **`https://epyykzxxglkjombvozhr.supabase.co/functions/v1/process-ended-topics-closing`**
   - **Request Method**：選 **POST**。
   - **Request Headers**：新增**兩筆**（Supabase 要求帶 Authorization，結語邏輯則認 x-cron-secret）：
     - **Name**：`Authorization` → **Value**：`Bearer 你的_ANON_KEY`  
       （從 Supabase Dashboard → **Project Settings** → **API** → **Project API keys** → **anon public** 複製整段，前面加 `Bearer ` 加一個空格。）
     - **Name**：`x-cron-secret` → **Value**：貼上 `secrets/cron_secret.txt` 裡的那一行密碼（與 Supabase CRON_SECRET 相同）。
   - **Schedule**：選「Every hour」或自訂，例如：
     - 每小時：`0 * * * *`
     - 每天 00:00 與 12:00：`0 0,12 * * *`
4. 儲存（Save）並啟用（Enable）。

### 建議頻率

- 每小時 1 次：結語約在主題結束後 1 小時內出現。
- 每天 1～2 次：可接受「當天內」出現即可時使用。

---

## 四、驗證是否成功

1. **手動觸發一次（建議）**  
   在 PowerShell 測試（請替換成你的 anon key 與 CRON_SECRET）：
   ```powershell
   curl.exe -X POST "https://epyykzxxglkjombvozhr.supabase.co/functions/v1/process-ended-topics-closing" -H "Authorization: Bearer 你的_ANON_KEY" -H "x-cron-secret: 你的CRON_SECRET" -H "Content-Type: application/json"
   ```
   - **anon key**：Supabase Dashboard → Project Settings → API → **anon public**。
   - **CRON_SECRET**：`secrets/cron_secret.txt` 裡的那一行。  
   回應應為 JSON，例如：`{"processed":0,"success":0,"failed":0,"results":[]}`（若目前沒有「已結束且未生成」的主題，會是 0 筆；若有則會顯示 processed / success 數量）。

2. **找一個已結束、尚未有結語的主題**  
   - 在 App 或網頁進入該主題詳情。
   - 應在「混亂結語」區塊看到「結語尚未生成，將在主題結束後由系統自動產生。」（或你設定的多語系文案）。

3. **等排程跑完或再手動觸發一次**  
   - 若該主題符合條件（已結束、`ai_summary_generated = false`），排程會為其生成結語並寫入 `topic_ai_summary`。
   - 重新進入該主題詳情或重新整理，應看到結語出現在同一區塊。

4. **檢查資料庫（可選）**  
   - Supabase Dashboard → **Table Editor** → `topic_ai_summary`：應能看到對應 `topic_id` 的新列。
   - `topics` 表：該主題的 `ai_summary_generated` 應變為 `true`。

---

## 五、檢查清單（你可逐項打勾）

| 步驟 | 說明 |
|------|------|
| ✅ | 在 Supabase Edge Functions Secrets 新增或確認 **CRON_SECRET**，並妥善保存其值。 |
| ✅ | 執行 `npx supabase functions deploy generate-ai-closing` 並部署成功。 |
| ☐ | 執行 migration `20260212000000_get_ended_topics_without_closing.sql`（建立 RPC `get_ended_topics_without_closing`），避免排程查詢出現 no results。 |
| ✅ | 執行 `npx supabase functions deploy process-ended-topics-closing` 並部署成功。 |
| ☐ | 在 cron-job.org（或其它排程）新增 POST 到 `process-ended-topics-closing`，並設定 **x-cron-secret** 與執行頻率。 |
| ☐ | 用 curl 或 Postman 手動 POST 一次，確認回應 200 且為合法 JSON。 |
| ☐ | 用一個已結束主題測試：先看到「結語尚未生成」，排程跑完後再開詳情應看到結語。 |

---

## 六、若遇到問題

- **401 Unauthorized**  
  檢查請求是否有帶 Header `x-cron-secret`，且值與 Supabase 裡 **CRON_SECRET** 完全一致（含大小寫、無多餘空白）。

- **結語一直沒出現**  
  1. 確認該主題的 `end_at` 已過、`ai_summary_generated` 為 false。  
  2. 確認排程有成功呼叫（看 cron 服務的執行紀錄）。  
  3. 到 Supabase Dashboard → Edge Functions → **Logs**，看 `process-ended-topics-closing` 與 `generate-ai-closing` 是否有錯誤。

- **已結束的主題超過一小時仍沒有補上結語**（依序檢查）  
  1. **排程有沒有在跑**  
     - 方案 A（pg_cron）：到 **Database** → **Cron Jobs**，或執行 `select * from cron.job_run_details where jobid = (select jobid from cron.job where jobname = 'process-ended-topics-closing') order by start_time desc limit 10;` 看最近是否有執行、是否有錯誤。  
     - 方案 B（cron-job.org）：登入後看該 Cron Job 的「執行紀錄」是否有每小時一筆、狀態是否成功。  
  2. **手動觸發一次看回應**  
     在 PowerShell 執行（請替換 ANON_KEY 與 CRON_SECRET）：  
     `curl.exe -X POST "https://epyykzxxglkjombvozhr.supabase.co/functions/v1/process-ended-topics-closing" -H "Authorization: Bearer 你的_ANON_KEY" -H "x-cron-secret: 你的CRON_SECRET" -H "Content-Type: application/json"`  
     若回應為 `{"processed":0,"success":0,"failed":0,"results":[]}` 且沒有 401，代表有被呼叫但「目前沒有符合條件的主題」；若有 `processed` > 0，看 `success` 與 `results` 裡各筆的 `error`。  
  3. **確認資料庫裡有沒有「應被處理」的主題**  
     在 **SQL Editor** 執行：  
     `select id, title, end_at, ai_summary_generated from topics where end_at <= now() and (ai_summary_generated = false or ai_summary_generated is null) limit 20;`  
     若有筆數，代表有已結束且未標記已生成的主題；若 0 筆，代表目前沒有需要補結語的主題（或都已生成）。  
  4. **看 Edge Function 日誌**  
     Supabase Dashboard → **Edge Functions** → 選 `process-ended-topics-closing` → **Logs**。  
     部署後日誌會出現：`找到 N 筆未生成結語的主題`、`排除已有結語後待處理: M 筆`、`完成: processed=... success=...`。若 N 或 M 為 0 但上面 SQL 有資料，可能是查詢條件或時區問題。  
  5. **確認並執行 RPC 與重新部署**  
     - 排程改為呼叫 RPC `get_ended_topics_without_closing`，不再用 PostgREST 的 `.or()` 查詢（可避免「no results found」或查無資料）。  
     - 需先執行 migration：`20260212000000_get_ended_topics_without_closing.sql`（在 Supabase Dashboard → SQL Editor 貼上該檔內容執行，或 `supabase db push`）。  
     - 再部署：`npx supabase functions deploy process-ended-topics-closing`。  
  6. **確認 generate-ai-closing 有 OPENAI_API_KEY**  
     Edge Functions 的 Secrets 需有 **OPENAI_API_KEY**，否則生成會失敗。

- **OPENAI_API_KEY 未設定**  
  結語由 `generate-ai-closing` 呼叫 OpenAI 生成，需在 Supabase Edge Functions 的 Secrets 裡設定 **OPENAI_API_KEY**。若之前已設過，通常不需再改。

完成以上步驟後，混亂結語就會在主題結束後由排程自發生成，並在主題詳情頁的結語區塊顯示。
