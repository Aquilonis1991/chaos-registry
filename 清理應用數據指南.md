# 清理應用數據指南

**問題**: `adb` 命令無法識別

---

## 🔧 解決方案

### 方法 1: 使用 Android Studio Terminal（最簡單）

1. **打開 Android Studio**
2. **打開 Terminal**（View → Tool Windows → Terminal）
3. **執行命令**:
   ```bash
   adb shell pm clear com.votechaos.app.debug
   ```

**優點**: Android Studio 會自動設置 PATH，無需配置

---

### 方法 2: 找到 adb 並使用完整路徑

#### 步驟 1: 找到 adb 的位置

通常位於以下位置之一：

```
C:\Users\<USERNAME>\AppData\Local\Android\Sdk\platform-tools\adb.exe
```

或

```
C:\Android\Sdk\platform-tools\adb.exe
```

#### 步驟 2: 使用完整路徑執行

在 PowerShell 中：

```powershell
# 替換為您的實際路徑
& "C:\Users\USER\AppData\Local\Android\Sdk\platform-tools\adb.exe" shell pm clear com.votechaos.app.debug
```

#### 步驟 3: 或臨時添加到 PATH

在當前 PowerShell 會話中：

```powershell
# 臨時添加到 PATH（僅當前會話有效）
$env:Path += ";C:\Users\USER\AppData\Local\Android\Sdk\platform-tools"

# 然後執行
adb shell pm clear com.votechaos.app.debug
```

---

### 方法 3: 在模擬器中手動清理（無需 adb）

1. **打開模擬器**
2. **打開「設置」應用**
3. **導航到「應用」**（或「Apps」）
4. **找到「VoteChaos」應用**
5. **點擊進入應用詳情**
6. **點擊「存儲」**（或「Storage」）
7. **點擊「清除數據」**（或「Clear Data」）
8. **確認清除**

---

### 方法 4: 使用 Android Studio 的 Device Manager

1. **打開 Android Studio**
2. **打開 Device Manager**（View → Tool Windows → Device Manager）
3. **找到您的模擬器**
4. **右鍵點擊模擬器**
5. **選擇「Wipe Data」**（擦除數據）

**注意**: 這會清除整個模擬器的數據，不只是應用數據

---

## 🔍 找到 adb 的完整路徑

### 在 PowerShell 中搜索

```powershell
# 搜索 adb.exe
Get-ChildItem -Path C:\Users\$env:USERNAME\AppData\Local\Android\Sdk -Recurse -Filter "adb.exe" -ErrorAction SilentlyContinue | Select-Object FullName
```

### 或檢查常見位置

```powershell
# 檢查常見位置
$possiblePaths = @(
    "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe",
    "C:\Android\Sdk\platform-tools\adb.exe",
    "$env:USERPROFILE\Android\Sdk\platform-tools\adb.exe"
)

foreach ($path in $possiblePaths) {
    if (Test-Path $path) {
        Write-Host "Found adb at: $path" -ForegroundColor Green
        Write-Host "Use: & `"$path`" shell pm clear com.votechaos.app.debug" -ForegroundColor Yellow
    }
}
```

---

## ✅ 推薦流程

### 最簡單的方法（推薦）

1. **使用 Android Studio Terminal**
   - 打開 Android Studio
   - 打開 Terminal
   - 執行: `adb shell pm clear com.votechaos.app.debug`

### 如果沒有 Android Studio Terminal

2. **在模擬器中手動清理**
   - 設置 → 應用 → VoteChaos → 存儲 → 清除數據

---

## 📝 驗證清理成功

清理後，重新啟動應用時應該：
- 應用重新初始化
- 所有緩存被清除
- 新的 JavaScript 文件被載入

在 Logcat 中應該看到：
```
[VoteChaos] MainActivity onCreate start
Loading app at https://localhost
Handling local request: https://localhost/assets/index-CGLgQaXY.js
```

**注意**: 文件名 hash 應該是新的（`index-CGLgQaXY.js`）

---

**最後更新**: 2025年1月
