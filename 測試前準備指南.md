# 測試前準備指南

## 🚀 快速開始

在執行測試前，請先執行準備腳本：

### Windows (PowerShell)

```powershell
cd votechaos-main
.\prepare-for-test.ps1
```

### Linux/Mac (Bash)

```bash
cd votechaos-main
./prepare-for-test.sh
```

---

## 📋 腳本執行內容

準備腳本會自動執行以下步驟：

### 1. 重新構建應用
- ✅ 構建前端 (`npm run build`)
- ✅ 同步 Capacitor (`npm run cap:sync:android`)
- ✅ 構建並安裝 Android 應用 (`gradlew assembleDebug installDebug`)

### 2. 清理應用數據
- ✅ 清理應用數據 (`adb shell pm clear com.votechaos.app.debug`)

---

## ⚙️ 手動執行（如果腳本不可用）

如果腳本無法執行，可以手動執行以下命令：

```bash
# 1. 重新構建應用
cd votechaos-main
npm run build
npm run cap:sync:android
cd android
./gradlew.bat assembleDebug installDebug  # Windows
# 或
./gradlew assembleDebug installDebug      # Linux/Mac
cd ..

# 2. 清理應用數據
adb shell pm clear com.votechaos.app.debug
```

---

## 🔍 驗證準備完成

準備完成後，您應該看到：

```
[OK] 應用重新構建完成
[OK] 應用數據已清理
[OK] 準備完成！
```

---

## 📝 下一步

準備完成後，您可以：

1. **啟動應用進行測試**
2. **查看 Logcat**:
   ```bash
   adb logcat -s VoteChaos
   ```
3. **按照測試流程進行測試**

---

## ⚠️ 注意事項

1. **確保模擬器已啟動**：腳本會檢查是否有已連接的設備/模擬器
2. **如果構建失敗**：檢查錯誤訊息並修復問題後重新執行
3. **如果清理失敗**：可能是應用未安裝，這是正常的（首次安裝時）

---

**最後更新**: 2025年1月
