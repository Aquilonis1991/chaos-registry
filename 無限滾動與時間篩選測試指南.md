# 無限滾動與時間篩選功能測試指南

## 📋 測試前準備

### 1. 確認環境
- [ ] 應用已正常啟動
- [ ] 已登入測試帳號
- [ ] 確認資料庫中有足夠的測試資料（至少 40+ 主題/記錄）

### 2. 開發者工具準備
- [ ] 打開瀏覽器開發者工具（F12）
- [ ] 切換到 **Network** 標籤（監控 API 請求）
- [ ] 切換到 **Console** 標籤（查看日誌）

---

## 🔄 第一部分：無限滾動功能測試

### 測試位置
- **首頁** (`/home`)
- 三個標籤頁：**熱門**、**最新**、**參與過**

### 測試步驟

#### 1. 測試「熱門」標籤的無限滾動

1. **進入首頁**
   - 導航到 `/home`
   - 確認當前在「熱門」標籤

2. **檢查初始載入**
   - [ ] 應該顯示前 20 個主題
   - [ ] 在列表底部看到載入指示器（Loader2 動畫）
   - [ ] Network 標籤中看到 `get_topics` RPC 請求，參數 `p_offset: 0`

3. **觸發無限滾動**
   - [ ] 滾動到頁面底部
   - [ ] 當載入指示器進入視窗時，應該自動觸發載入更多
   - [ ] Network 標籤中看到新的 `get_topics` 請求，參數 `p_offset: 20`
   - [ ] 新主題應該追加到列表底部（不是替換）

4. **繼續滾動**
   - [ ] 再次滾動到底部
   - [ ] 應該繼續載入更多（`p_offset: 40`, `p_offset: 60`...）
   - [ ] 每次載入 20 個主題

5. **檢查載入狀態**
   - [ ] 載入中時，底部顯示載入動畫
   - [ ] 載入完成後，動畫消失
   - [ ] 如果沒有更多資料，載入指示器應該消失

#### 2. 測試「最新」標籤的無限滾動

1. **切換標籤**
   - [ ] 點擊「最新」標籤
   - [ ] 列表應該重置並顯示最新的 20 個主題

2. **重複滾動測試**
   - [ ] 滾動到底部
   - [ ] 確認自動載入更多
   - [ ] 確認新資料追加到列表

#### 3. 測試「參與過」標籤的無限滾動

1. **切換標籤**
   - [ ] 點擊「參與過」標籤
   - [ ] 如果用戶有參與過的主題，應該顯示

2. **滾動測試**
   - [ ] 滾動到底部
   - [ ] 確認無限滾動正常工作
   - [ ] 確認使用正確的 offset 參數

#### 4. 邊界情況測試

1. **資料不足 20 條**
   - [ ] 如果某個標籤只有 10 個主題
   - [ ] 應該只顯示 10 個，不顯示載入指示器

2. **沒有更多資料**
   - [ ] 滾動到最後一頁
   - [ ] 載入指示器應該消失
   - [ ] 不會再發送 API 請求

3. **快速滾動**
   - [ ] 快速連續滾動到底部
   - [ ] 不應該重複發送請求
   - [ ] 應該正確處理 `loadingMore` 狀態

### 預期行為

✅ **正常情況**：
- 初始載入 20 個主題
- 滾動到底部自動載入更多
- 新資料追加到列表底部
- 載入中顯示動畫
- 沒有更多資料時停止載入

❌ **不應該出現**：
- 重複載入相同資料
- 載入時替換現有列表
- 無限循環載入
- 載入指示器一直顯示

---

## ⏰ 第二部分：時間篩選功能測試

### 測試位置
- **投票紀錄** (`/profile` → 投票紀錄)
- **主題發起紀錄** (`/profile` → 主題發起紀錄)
- **代幣使用紀錄** (`/profile` → 代幣使用紀錄)

### 測試步驟

#### 1. 測試投票紀錄的時間篩選

1. **進入投票紀錄頁面**
   - [ ] 導航到個人資料頁
   - [ ] 點擊「投票紀錄」
   - [ ] 確認右上角有時間篩選按鈕（顯示「全部」或當前選擇）

2. **測試「全部」選項**
   - [ ] 點擊時間篩選按鈕
   - [ ] 選擇「全部」
   - [ ] 應該顯示所有投票記錄（非管理員限制在1年內）
   - [ ] Network 標籤中看到 API 請求，沒有時間篩選參數

3. **測試「1天」篩選**
   - [ ] 點擊時間篩選按鈕
   - [ ] 選擇「1天」
   - [ ] 應該只顯示最近1天的投票記錄
   - [ ] Network 標籤中看到 `created_at >= [1天前的日期]` 的查詢條件
   - [ ] 列表應該立即更新

4. **測試其他時間範圍**
   - [ ] 測試「1週」：只顯示最近7天的記錄
   - [ ] 測試「1個月」：只顯示最近30天的記錄
   - [ ] 測試「3個月」：只顯示最近90天的記錄
   - [ ] 測試「6個月」：只顯示最近180天的記錄
   - [ ] 測試「1年」：只顯示最近365天的記錄

5. **檢查篩選結果**
   - [ ] 每個時間範圍的記錄數量應該遞減
   - [ ] 記錄的時間應該在選擇的時間範圍內
   - [ ] 最舊的記錄不應該超過選擇的時間範圍

#### 2. 測試主題發起紀錄的時間篩選

1. **進入主題發起紀錄頁面**
   - [ ] 從個人資料頁點擊「主題發起紀錄」
   - [ ] 確認右上角有時間篩選按鈕

2. **重複時間篩選測試**
   - [ ] 測試所有時間範圍選項
   - [ ] 確認篩選正確應用
   - [ ] 確認列表正確更新

#### 3. 測試代幣使用紀錄的時間篩選

1. **進入代幣使用紀錄頁面**
   - [ ] 從個人資料頁點擊「代幣使用紀錄」
   - [ ] 確認右上角有時間篩選按鈕

2. **重複時間篩選測試**
   - [ ] 測試所有時間範圍選項
   - [ ] 確認篩選正確應用
   - [ ] 確認交易記錄正確顯示

#### 4. 管理員權限測試

1. **使用管理員帳號登入**
   - [ ] 確認帳號有管理員權限

2. **測試1年以前的資料**
   - [ ] 選擇「全部」選項
   - [ ] 應該可以查看1年以前的資料
   - [ ] 確認沒有時間限制

3. **使用非管理員帳號**
   - [ ] 切換到一般用戶帳號
   - [ ] 選擇「全部」選項
   - [ ] 應該只能查看1年內的資料
   - [ ] 即使選擇「全部」，查詢條件也應該包含 `created_at >= [1年前]`

### 預期行為

✅ **正常情況**：
- 時間篩選按鈕顯示在右上角
- 點擊按鈕顯示下拉選單
- 選擇時間範圍後立即更新列表
- 篩選結果正確（只顯示選擇時間範圍內的記錄）
- 非管理員自動限制在1年內

❌ **不應該出現**：
- 篩選後顯示錯誤時間範圍的資料
- 篩選按鈕無法點擊
- 選擇篩選後列表不更新
- 非管理員可以查看1年以前的資料

---

## 🔍 第三部分：組合測試

### 測試場景

1. **切換標籤 + 時間篩選**
   - [ ] 在投票紀錄頁面選擇「1週」
   - [ ] 切換到主題發起紀錄
   - [ ] 時間篩選應該重置為「全部」
   - [ ] 選擇新的時間範圍
   - [ ] 切換回投票紀錄
   - [ ] 時間篩選應該保持之前的選擇（「1週」）

2. **快速切換時間範圍**
   - [ ] 快速連續選擇不同的時間範圍
   - [ ] 確認每次選擇都正確應用
   - [ ] 不應該出現重複請求或錯誤狀態

3. **空結果處理**
   - [ ] 選擇一個時間範圍，但該範圍內沒有資料
   - [ ] 應該顯示空狀態提示
   - [ ] 不應該顯示錯誤訊息

---

## 🐛 常見問題排查

### 無限滾動問題

**問題：滾動到底部沒有載入更多**
- [ ] 檢查 Network 標籤，確認是否有 API 請求
- [ ] 檢查 Console 是否有錯誤訊息
- [ ] 確認 `hasMore` 狀態是否為 `true`
- [ ] 確認 Intersection Observer 是否正常工作

**問題：重複載入相同資料**
- [ ] 檢查 `offset` 是否正確遞增
- [ ] 檢查 `loadingMore` 狀態是否正確管理
- [ ] 確認 `useEffect` 依賴項是否正確

### 時間篩選問題

**問題：篩選後沒有更新**
- [ ] 檢查 Network 標籤，確認 API 請求是否包含時間條件
- [ ] 檢查 Console 是否有錯誤訊息
- [ ] 確認 `timeFilter` 狀態是否正確更新
- [ ] 確認 hook 的 `useEffect` 是否觸發

**問題：非管理員可以查看1年以前的資料**
- [ ] 檢查 `isAdmin` 狀態是否正確
- [ ] 檢查 API 請求是否包含 `created_at >= [1年前]` 條件
- [ ] 確認 hook 中的權限檢查邏輯

---

## 📊 測試檢查清單

### 無限滾動
- [ ] 熱門標籤無限滾動正常
- [ ] 最新標籤無限滾動正常
- [ ] 參與過標籤無限滾動正常
- [ ] 載入指示器正確顯示/隱藏
- [ ] 新資料正確追加到列表
- [ ] 沒有更多資料時停止載入
- [ ] 快速滾動不會重複請求

### 時間篩選
- [ ] 投票紀錄時間篩選正常
- [ ] 主題發起紀錄時間篩選正常
- [ ] 代幣使用紀錄時間篩選正常
- [ ] 所有時間範圍選項正常工作
- [ ] 篩選結果正確
- [ ] 非管理員限制在1年內
- [ ] 管理員可以查看所有資料

---

## 🎯 測試完成標準

### 無限滾動
✅ 所有三個標籤頁都能正常無限滾動  
✅ 載入狀態正確顯示  
✅ 資料正確追加，不重複  
✅ 邊界情況正確處理  

### 時間篩選
✅ 三個歷史記錄頁面都能正常篩選  
✅ 所有時間範圍選項正常工作  
✅ 篩選結果準確  
✅ 權限控制正確  

---

## 💡 測試技巧

1. **使用開發者工具**
   - Network 標籤：監控 API 請求和參數
   - Console 標籤：查看錯誤和日誌
   - Elements 標籤：檢查 DOM 結構

2. **測試資料準備**
   - 確保有足夠的測試資料（至少 40+ 記錄）
   - 確保有不同時間範圍的資料
   - 確保有1年以前和1年內的資料（測試權限）

3. **逐步測試**
   - 先測試基本功能
   - 再測試邊界情況
   - 最後測試組合場景

4. **記錄問題**
   - 記錄任何異常行為
   - 截圖保存問題狀態
   - 記錄 Console 錯誤訊息

---

## 📝 測試報告模板

```
測試日期：__________
測試人員：__________
測試環境：__________

### 無限滾動測試結果
- 熱門標籤：✅/❌
- 最新標籤：✅/❌
- 參與過標籤：✅/❌
- 問題記錄：__________

### 時間篩選測試結果
- 投票紀錄：✅/❌
- 主題發起紀錄：✅/❌
- 代幣使用紀錄：✅/❌
- 權限控制：✅/❌
- 問題記錄：__________

### 總體評價
- 功能完整性：__________
- 用戶體驗：__________
- 需要改進：__________
```

---

**測試完成後，請記錄測試結果並回報任何發現的問題！** 🎉
