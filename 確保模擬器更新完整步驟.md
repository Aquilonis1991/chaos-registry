# 確保模擬器更新完整步驟

**問題**: 應用已重新構建和安裝，但模擬器中看不到變化

---

## 🔄 完整更新流程

### 步驟 1: 完全關閉應用（必須）

1. **在模擬器中**
   - 按返回鍵多次，直到完全退出應用
   - 或使用「最近使用的應用」按鈕，向上滑動關閉應用

2. **確認應用已關閉**
   - 應用不應該在背景運行

---

### 步驟 2: 清理應用數據和緩存（重要）

#### 方法 A: 使用 ADB（推薦）

在 Android Studio Terminal 或 PowerShell 中執行：

```bash
# 清理應用數據（這會清除所有緩存，包括 WebView 緩存）
adb shell pm clear com.votechaos.app.debug
```

**預期輸出**: `Success`

#### 方法 B: 在模擬器中手動清理

1. 打開「設置」應用
2. 導航到「應用」→「VoteChaos」
3. 點擊「存儲」
4. 點擊「清除數據」
5. 點擊「清除緩存」
6. 確認清除

---

### 步驟 3: 強制停止應用（確保完全關閉）

```bash
# 強制停止應用
adb shell am force-stop com.votechaos.app.debug
```

---

### 步驟 4: 重新啟動應用

#### 方法 A: 在模擬器中點擊應用圖標

#### 方法 B: 使用命令啟動

```bash
# 啟動應用
adb shell am start -n com.votechaos.app.debug/.MainActivity
```

---

### 步驟 5: 驗證新版本已載入

#### 檢查 Logcat

```bash
# 過濾 VoteChaos 標籤
adb logcat -s VoteChaos
```

**應該看到**:
```
[VoteChaos] MainActivity onCreate start
[VoteChaos] MainActivity onCreate complete
```

#### 檢查 JavaScript 文件版本

在 Logcat 中查找：
```
Loading app at https://localhost
Handling local request: https://localhost/assets/index-*.js
```

**注意**: 文件名中的 hash 應該與最新構建匹配：
- 最新構建: `index-CGLgQaXY.js`
- 如果看到舊的 hash（如 `index-wNI0-Nv-.js`），說明緩存未清除

---

## ⚠️ 如果仍然沒有變化

### 解決方案 1: 完全重置模擬器

1. **在 Android Studio 中**
   - 打開 AVD Manager
   - 找到您的模擬器
   - 點擊「Wipe Data」按鈕（🗑️）
   - 確認擦除

2. **重新啟動模擬器**
   - 點擊「Play」按鈕啟動模擬器
   - 等待模擬器完全啟動

3. **重新安裝應用**
   ```bash
   cd android
   ./gradlew.bat installDebug
   ```

---

### 解決方案 2: 檢查 WebView 緩存

WebView 可能會緩存 JavaScript 文件。強制清除：

```bash
# 清除 WebView 緩存
adb shell pm clear com.google.android.webview
```

**注意**: 這會清除所有應用的 WebView 緩存

---

### 解決方案 3: 驗證文件確實已更新

#### 檢查 dist 目錄

```bash
# 查看最新構建的文件
ls -la dist/assets/index-*.js
```

應該看到 `index-CGLgQaXY.js`（或類似的 hash）

#### 檢查 Android assets

```bash
# 查看 Android assets 中的文件
ls -la android/app/src/main/assets/public/assets/index-*.js
```

應該與 dist 目錄中的文件一致

---

### 解決方案 4: 強制重新構建（不使用任何緩存）

```bash
# 1. 完全清理
cd votechaos-main
Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force android\app\build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force android\app\src\main\assets\public -ErrorAction SilentlyContinue

# 2. 重新構建
npm run build

# 3. 同步 Capacitor
npm run cap:sync:android

# 4. 清理並重新構建 Android
cd android
.\gradlew.bat clean
.\gradlew.bat assembleDebug --rerun-tasks

# 5. 卸載舊版本
.\gradlew.bat uninstallDebug

# 6. 安裝新版本
.\gradlew.bat installDebug
```

---

## ✅ 驗證清單

### 必須確認

- [ ] ✅ 應用已完全關閉（不在背景運行）
- [ ] ✅ 應用數據已清理（`adb shell pm clear`）
- [ ] ✅ 應用已重新啟動
- [ ] ✅ Logcat 顯示新的文件 hash
- [ ] ✅ 應用行為符合預期

---

## 🔍 調試技巧

### 1. 檢查應用版本

```bash
# 查看應用安裝時間
adb shell dumpsys package com.votechaos.app.debug | grep "firstInstallTime\|lastUpdateTime"
```

### 2. 檢查文件時間戳

```bash
# 查看 APK 構建時間
ls -la android/app/build/outputs/apk/debug/app-debug.apk
```

### 3. 查看 WebView 載入的文件

在 Logcat 中搜索：
```
Handling local request: https://localhost/assets/
```

確認文件名與最新構建匹配

---

## 📝 常見問題

### Q: 為什麼應用還是顯示舊版本？

**A**: 可能原因：
1. WebView 緩存了舊的 JavaScript 文件
2. 應用數據未完全清理
3. 應用仍在背景運行，未完全重啟

**解決**: 執行「步驟 2: 清理應用數據和緩存」

---

### Q: 如何確認新版本已載入？

**A**: 檢查 Logcat 中的文件 hash：
- 最新構建: `index-CGLgQaXY.js`
- 如果看到舊的 hash，說明緩存未清除

---

### Q: 構建成功但應用沒有變化？

**A**: 可能原因：
1. Capacitor 同步未正確執行
2. Android assets 未更新
3. 應用未重新安裝

**解決**: 執行「解決方案 4: 強制重新構建」

---

**最後更新**: 2025年1月
