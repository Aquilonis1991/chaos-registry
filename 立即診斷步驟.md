# Twitter 登入問題立即診斷步驟

**問題**: 授權後回到網頁版登入頁，而不是應用

---

## 🚨 立即檢查（5分鐘）

### 步驟 1: 檢查 Edge Function 日誌（最重要）

1. **登入 Supabase Dashboard**
   - 網址: https://supabase.com/dashboard
   - 選擇您的專案

2. **導航到 Edge Functions**
   - 左側選單 → Edge Functions → twitter-auth

3. **查看日誌**
   - 點擊「Logs」標籤
   - 查找最新的日誌（應該在您完成授權時產生）

4. **查找關鍵日誌**

**應該看到的日誌**（如果正常）:
```
[CRITICAL] GET callback from Supabase standard callback URL
[CRITICAL] Detected platform from state: app
[CRITICAL] App platform detected, redirecting to Deep Link
[CRITICAL] Redirecting to Deep Link: votechaos://auth/callback?code=...&state=...
```

**如果看到這些日誌**（表示問題）:
```
[CRITICAL] GET callback from Supabase standard callback URL
[CRITICAL] Failed to verify state, using default platform detection
[CRITICAL] Web platform detected, redirecting to frontend callback URL
```

或

```
[CRITICAL] GET callback from Supabase standard callback URL
[CRITICAL] Detected platform from state: auto
[CRITICAL] Web platform detected, redirecting to frontend callback URL
```

---

### 步驟 2: 檢查環境變數

1. **在 Supabase Dashboard**
   - Edge Functions → twitter-auth → Settings

2. **檢查環境變數**:
   - `FRONTEND_DEEP_LINK`: 應該是 `votechaos://auth/callback`
   - `FRONTEND_URL`: 應該是您的網頁版 URL

3. **如果環境變數未設置**:
   - 點擊「Add new secret」
   - 添加 `FRONTEND_DEEP_LINK` = `votechaos://auth/callback`
   - 添加 `FRONTEND_URL` = 您的網頁版 URL
   - 點擊「Save」

4. **重新部署 Edge Function**:
   ```bash
   cd votechaos-main
   npx supabase functions deploy twitter-auth
   ```

---

### 步驟 3: 檢查 State 驗證

如果 Edge Function 日誌顯示「Failed to verify state」，可能是：

1. **JWT_SECRET 未設置**
   - 在 Supabase Dashboard → Edge Functions → twitter-auth → Settings
   - 檢查是否有 `JWT_SECRET` 環境變數
   - 如果沒有，需要從 Supabase Dashboard → Settings → API → JWT Secret 獲取

2. **State 過期**
   - State 有效期是 10 分鐘
   - 如果授權過程超過 10 分鐘，state 會過期

---

## 🔧 快速修復

### 修復 1: 重新部署 Edge Function

```bash
cd votechaos-main
npx supabase functions deploy twitter-auth
```

**確保**:
- 使用最新的代碼
- 環境變數已正確設置

---

### 修復 2: 設置環境變數

在 Supabase Dashboard → Edge Functions → twitter-auth → Settings：

**必須設置**:
- `FRONTEND_DEEP_LINK`: `votechaos://auth/callback`
- `FRONTEND_URL`: 您的網頁版 URL（例如：`https://chaos-registry.vercel.app`）
- `JWT_SECRET`: 從 Supabase Dashboard → Settings → API → JWT Secret 獲取

---

### 修復 3: 驗證 State 生成

檢查 Edge Function 代碼中 `generateSignedState` 函數：

在 `supabase/functions/twitter-auth/index.ts` 中，應該看到：
```typescript
const payload = {
  timestamp: Date.now(),
  platform: 'app', // 必須是 'app'
  codeVerifier: codeVerifier,
  provider: 'twitter',
  exp: Math.floor(Date.now() / 1000) + expiresIn,
}
```

**確認** `platform` 是 `'app'`（不是 `'auto'` 或其他值）

---

## 📊 診斷結果對照表

| Edge Function 日誌 | 問題 | 解決方案 |
|-------------------|------|---------|
| `Detected platform from state: app` + `Redirecting to Deep Link` | Edge Function 正常，可能是 WebView 問題 | 檢查 WebView 日誌 |
| `Failed to verify state` | State 驗證失敗 | 檢查 JWT_SECRET 環境變數 |
| `Detected platform from state: auto` | Platform 檢測失敗 | 檢查 state 生成邏輯 |
| `Web platform detected` | 沒有檢測到 app platform | 檢查 state 中的 platform 值 |

---

## 🎯 下一步

**請提供以下信息**:

1. **Edge Function 日誌**（從 Supabase Dashboard）
   - 複製最新的日誌（特別是包含 `[CRITICAL]` 的行）

2. **環境變數設置**（截圖或列表）
   - `FRONTEND_DEEP_LINK`
   - `FRONTEND_URL`
   - `JWT_SECRET`

3. **Logcat 日誌**（如果可能）
   - 特別是授權完成後的日誌

---

**最後更新**: 2025年1月
