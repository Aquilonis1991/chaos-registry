# 第三方登入流程說明與修復（X / LINE）

## 一、X（Twitter）登入錯誤：「你無法將存取權授予此應用程式」

### 現象
點擊「使用 X (Twitter) 登入」後，跳轉到 X 授權頁面時出現：
- **你無法將存取權授予此應用程式。請返回並嘗試重新登入。**
- 或 X 頁面提示隱私相關擴充功能可能造成問題。

### 原因說明
X OAuth 2.0 要求 **redirect_uri** 必須與 **X Developer Portal** 中設定的 Callback URL **完全一致**，否則會拒絕授權（常見 403）。

您目前請求中的 URL 為：
- **redirect_uri**（X 回傳 code 的位址）：`https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback`
- **redirect_to**（登入成功後要去的網址）：`https://chaos-registry.vercel.app/auth/callback`

### 解決步驟（X Developer Portal）

1. 前往 [X Developer Portal](https://developer.x.com/) → 選擇您的 App → **App 設定**（或 User authentication settings）。
2. 在 **Callback URI / Redirect URLs** 中新增（或確認已有）：
   ```text
   https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback
   ```
   - 必須與請求中的 `redirect_uri` **一字不差**（含 https、無尾端斜線，除非請求也有）。
   - 每個 App 最多 10 組 Callback URL。
3. 儲存設定後，再試一次「使用 X 登入」。

### 其他注意事項（X）
- **Scope**：目前程式已簡化為 `users.read users.email offline.access`（已移除 `tweet.read`），若仍被拒可再檢查 App 權限等級。
- **隱私擴充功能**：若在電腦瀏覽器測試，可暫時關閉擋追蹤／擋第三方 Cookie 的擴充功能再試。
- **Supabase 設定**：  
  Dashboard → Authentication → URL Configuration：  
  - **Site URL**：例如 `https://chaos-registry.vercel.app`  
  - **Redirect URLs**：需包含 `https://chaos-registry.vercel.app/auth/callback`（或您實際使用的登入後跳轉網址）。

參考：[X Callback URLs 說明](https://developer.x.com/en/docs/apps/callback-urls)

---

## 二、LINE 登入：授權後回到 App 只看到登入畫面

### 現象
在 App 內點「LINE 登入」→ 到 LINE 授權並同意 → 回到 App 後仍停在登入頁，沒有完成登入。

### 原因說明
LINE 登入在 App 內是透過 **Deep Link**（`votechaos://auth/callback?...`）回到應用。  
原先 **Deep Link 監聽器（appUrlOpen）** 是在 `main.tsx` 的 **500ms 延遲** 後才註冊；若使用者在授權後很快被導回 App，或從背景/冷啟動被 Deep Link 喚起，事件可能在監聽器註冊前就觸發，導致 **回調被漏接**，畫面只會停在登入頁。

### 已做修復（程式面）
- **提早註冊 Deep Link 監聽器**：在 `main.tsx` 中，改為在 React 渲染後**立即**以 `import("./lib/app-lifecycle").then(...)` 呼叫 `initializeAppLifecycle()`，不再等 500ms。
- 這樣在 App 被 `votechaos://auth/callback?code=...&state=...&provider=line` 喚起時，能盡早收到 `appUrlOpen`，並由 `OAuthCallbackHandler` 用 code/state 呼叫 `line-auth` Edge Function 換 token、建立 session 並導向首頁。

### 請您再確認（LINE）
1. **重新建置並安裝 App**  
   修改在 `main.tsx`，需重新 `npm run build` 並在 Android Studio 中跑一次實機/模擬器，確保載入的是新版本。
2. **LINE 回調網址**  
   LINE Developers 後台「LINE Login」的 **Callback URL** 應為：
   ```text
   https://epyykzxxglkjombvozhr.supabase.co/functions/v1/line-auth-callback
   ```
   （與專案中 `LINE_REDIRECT_URI` / Edge Function 的 URL 一致。）
3. **Edge Function 環境變數**  
   `line-auth`、`line-auth-callback` 的 `FRONTEND_DEEP_LINK` 應為 `votechaos://auth/callback`（預設已是）。

若仍只回到登入頁，可看 Android Logcat 中是否有 `[app-lifecycle] DEEP LINK RECEIVED`、`[OAuthCallbackHandler]` 等日誌，以確認 Deep Link 是否有被收到、以及後續是否有呼叫 Edge Function。

---

## 三、流程對照簡表

| 項目 | X (Twitter) | LINE（App） |
|------|-------------|-------------|
| 觸發 | `signInWithOAuth({ provider: 'x', options: { redirectTo, scopes } })` | 呼叫 `line-auth` Edge Function 取得 authUrl，再 `window.location.href = authUrl` |
| 授權後 | X 重導向到 Supabase `.../auth/v1/callback`，Supabase 換 token 後再導向 `redirectTo`（網頁）或 Deep Link（App） | LINE 重導向到 `line-auth-callback` Edge Function，再 302 到 `votechaos://auth/callback?code=...&state=...&provider=line` |
| App 收到 | Supabase 內建處理；若用 Deep Link 則同樣由 appUrlOpen → oauth-callback → OAuthCallbackHandler | appUrlOpen → app-lifecycle 派發 oauth-callback → OAuthCallbackHandler 用 code/state POST 到 `line-auth/callback` 換 token → setSession / verifyOtp → 導向 /home |
| 關鍵設定 | X Developer Portal 的 Callback URL = `https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback` | LINE 後台 Callback URL = `https://...supabase.co/functions/v1/line-auth-callback`；App 需盡早註冊 appUrlOpen（已修復） |

---

## 四、檢查清單

### X 登入
- [ ] X Developer Portal 已新增 `https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback`
- [ ] Supabase Authentication URL 設定中 Site URL / Redirect URLs 與實際前端網址一致
- [ ] 若在瀏覽器測試，可暫時關閉隱私/擋追蹤擴充功能

### LINE 登入（App）
- [ ] 已重新 build 並安裝 App（含 main.tsx 的提早註冊修改）
- [ ] LINE Login Channel 的 Callback URL = `https://epyykzxxglkjombvozhr.supabase.co/functions/v1/line-auth-callback`
- [ ] Edge Function `LINE_REDIRECT_URI`、`FRONTEND_DEEP_LINK` 與上一致
- [ ] 若仍失敗，查看 Logcat 中 `[app-lifecycle]`、`[OAuthCallbackHandler]`、`[line-auth-callback]` 日誌

---

**文件更新**：依本次 X 錯誤說明與 LINE 回調遺失修復更新。
