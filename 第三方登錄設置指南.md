# 第三方登錄設置指南

## 📋 概述

目前前端已經實現了 Google、Apple 和 Discord 登錄的 UI，但需要在 Supabase 後台配置 OAuth providers 才能正常運作。

### 支持的第三方登錄提供商

1. **Google** - 最常用，設置簡單
2. **Apple** - iOS 用戶首選（需要付費 Apple Developer 帳號）
3. **Discord** - 遊戲社群常用，設置簡單且免費 ⭐ 推薦
4. **Facebook** - 社群媒體常用，設置簡單
5. **LINE** - 台灣、日本、泰國等地區常用 ⭐ 推薦

## 🔧 設置步驟

### 1. Google OAuth 設置

#### 步驟 1.1：創建 Google OAuth 憑證

1. 前往 [Google Cloud Console](https://console.cloud.google.com/)
2. 選擇或創建一個專案
3. 啟用 **Google+ API**：
   - 導航到「API 和服務」>「程式庫」
   - 搜尋「Google+ API」並啟用
4. 創建 OAuth 2.0 憑證：
   - 導航到「API 和服務」>「憑證」
   - 點擊「建立憑證」>「OAuth 用戶端 ID」
   - 應用程式類型選擇「網頁應用程式」
   - 授權的重新導向 URI 添加：
     ```
     https://<your-project-ref>.supabase.co/auth/v1/callback
     ```
     > 將 `<your-project-ref>` 替換為您的 Supabase 專案引用 ID（可在 Supabase Dashboard > Settings > API 中找到）

#### 步驟 1.2：在 Supabase 中配置 Google Provider

1. 登入 [Supabase Dashboard](https://app.supabase.com/)
2. 選擇您的專案
3. 導航到 **Authentication** > **Providers**
4. 找到 **Google** 並點擊啟用
5. 填入以下資訊：
   - **Client ID (for OAuth)**: 從 Google Cloud Console 複製
   - **Client Secret (for OAuth)**: 從 Google Cloud Console 複製
6. 點擊 **Save**

---

### 2. Apple Sign In 設置

#### 步驟 2.1：創建 Apple Developer 帳號和服務 ID

1. 前往 [Apple Developer Portal](https://developer.apple.com/)
2. 登入您的 Apple Developer 帳號（需要付費會員資格）
3. 創建服務 ID：
   - 導航到「Certificates, Identifiers & Profiles」
   - 選擇「Identifiers」>「Services IDs」
   - 點擊「+」創建新的服務 ID
   - 填寫描述和標識符（例如：`com.yourcompany.votechaos`）
   - 勾選「Sign in with Apple」
   - 配置回調 URL：
     ```
     https://<your-project-ref>.supabase.co/auth/v1/callback
     ```
     > 將 `<your-project-ref>` 替換為您的 Supabase 專案引用 ID

#### 步驟 2.2：創建密鑰

1. 在 Apple Developer Portal 中：
   - 導航到「Keys」
   - 點擊「+」創建新密鑰
   - 命名密鑰（例如：`Supabase Sign In with Apple`）
   - 勾選「Sign in with Apple」
   - 點擊「Configure」並選擇您的服務 ID
   - 點擊「Continue」>「Register」
   - **下載密鑰文件（.p8）**（只能下載一次！）
   - 記下 **Key ID**

#### 步驟 2.3：在 Supabase 中配置 Apple Provider

1. 在 Supabase Dashboard 中：
   - 導航到 **Authentication** > **Providers**
   - 找到 **Apple** 並點擊啟用
2. 填入以下資訊：
   - **Services ID**: 您創建的服務 ID（例如：`com.yourcompany.votechaos`）
   - **Secret Key**: 從下載的 .p8 文件中複製
   - **Key ID**: 從 Apple Developer Portal 複製
   - **Team ID**: 在 Apple Developer Portal 右上角可以找到（例如：`ABC123DEF4`）
3. 點擊 **Save**

---

### 3. Facebook Login 設置

#### 步驟 3.1：創建 Facebook App

1. 前往 [Facebook Developers](https://developers.facebook.com/)
2. 點擊右上角的 **「我的應用程式」** > **「建立應用程式」**
3. 選擇應用程式類型：選擇 **「消費者」** 或 **「商業」**
4. 填寫應用程式資訊：
   - **應用程式顯示名稱**：例如 `VoteChaos`
   - **應用程式聯絡電子郵件**：您的電子郵件
   - 點擊 **「建立應用程式」**
5. 添加 Facebook Login 產品：
   - 在應用程式儀表板中，找到 **「Facebook 登入」** 產品
   - 點擊 **「設定」**
6. 配置 OAuth 重定向 URI：
   - 在左側選單中，點擊 **「設定」** > **「基本」**
   - 在 **「有效的 OAuth 重新導向 URI」** 區塊中，點擊 **「新增 URI」**
   - 添加：
     ```
     https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback
     ```
     > 將 `epyykzxxglkjombvozhr` 替換為您的 Supabase 專案引用 ID
   - 點擊 **「儲存變更」**
7. 獲取 App ID 和 App Secret：
   - 在 **「設定」** > **「基本」** 頁面中
   - 您會看到 **「應用程式編號」**（App ID）
   - 點擊 **「顯示」** 來查看 **「應用程式密鑰」**（App Secret）

#### 步驟 3.2：在 Supabase 中配置 Facebook Provider

1. 登入 [Supabase Dashboard](https://app.supabase.com/)
2. 選擇您的專案
3. 導航到 **Authentication** > **Providers**
4. 找到 **Facebook** 並點擊 **「Enable」** 或切換開關啟用
5. 填入以下資訊：
   - **Client ID (for OAuth)**: 從 Facebook App 設定中複製的 App ID
   - **Client Secret (for OAuth)**: 從 Facebook App 設定中複製的 App Secret
6. 點擊 **「Save」**

#### 步驟 3.3：Facebook App 審核（生產環境）

**注意**：Facebook 要求應用程式通過審核才能在生產環境中使用。開發階段可以添加測試用戶。

1. 在 Facebook App 設定中：
   - 導航到 **「角色」** > **「角色」**
   - 添加測試用戶的 Facebook 帳號
2. 測試用戶可以使用 Facebook 登錄，但一般用戶需要等待審核通過

---

### 4. Discord OAuth 設置 ⭐ 推薦（免費且簡單）

#### 步驟 4.1：創建 Discord Application

1. 前往 [Discord Developer Portal](https://discord.com/developers/applications)
2. 點擊右上角的 **「New Application」** 按鈕
3. 填寫應用程式名稱（例如：`VoteChaos`）
4. 點擊 **「Create」**
5. 在左側選單中，點擊 **「OAuth2」**
6. 在 **「Redirects」** 區塊中，點擊 **「Add Redirect」**
7. 添加重定向 URL：
   ```
   https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback
   ```
   > 將 `epyykzxxglkjombvozhr` 替換為您的 Supabase 專案引用 ID
8. 點擊 **「Save Changes」**
9. 在 **「OAuth2」** 頁面頂部，您會看到：
   - **CLIENT ID**：複製此值
   - **CLIENT SECRET**：點擊 **「Reset Secret」** 或 **「Copy」** 來獲取（如果沒有顯示，點擊 **「Reset Secret」** 生成新的）

#### 步驟 4.2：在 Supabase 中配置 Discord Provider

1. 登入 [Supabase Dashboard](https://app.supabase.com/)
2. 選擇您的專案
3. 導航到 **Authentication** > **Providers**
4. 找到 **Discord** 並點擊 **「Enable」** 或切換開關啟用
5. 填入以下資訊：
   - **Client ID (for OAuth)**: 從 Discord Developer Portal 複製的 CLIENT ID
   - **Client Secret (for OAuth)**: 從 Discord Developer Portal 複製的 CLIENT SECRET
6. 點擊 **「Save」**

#### 步驟 4.3：Discord OAuth 權限範圍（可選）

Discord OAuth 預設會請求以下權限：
- `identify`: 獲取用戶基本資訊（用戶名、頭像等）
- `email`: 獲取用戶電子郵件（如果用戶已驗證）

這些權限已經足夠，無需額外配置。

#### 步驟 4.4：測試 Discord 登錄

1. 在應用中點擊「使用 Discord 登入」按鈕
2. 應該會跳轉到 Discord 授權頁面
3. 點擊 **「Authorize」** 授權應用
4. 登錄成功後應該會重定向回應用

---

### 5. LINE Login 設置 ⭐ 推薦（台灣、日本、泰國等地區常用）

#### 步驟 5.1：創建 LINE Channel

1. 前往 [LINE Developers Console](https://developers.line.biz/console/)
2. 登入您的 LINE 帳號
3. 點擊 **「建立」** 創建新的 Provider（如果還沒有）
4. 在 Provider 中，點擊 **「建立」** 創建新的 Channel
5. 選擇 Channel 類型：
   - 選擇 **「LINE Login」**
6. 填寫 Channel 資訊：
   - **Channel 名稱**：例如 `VoteChaos`
   - **Channel 說明**：應用程式描述
   - **應用程式網址**：您的應用網址（例如：`https://yourdomain.com`）
   - **應用程式圖示**：上傳應用程式圖示（可選）
   - 點擊 **「建立」**
7. 配置 Callback URL：
   - 在 Channel 設定頁面中，找到 **「Callback URL」**
   - 添加：
     ```
     https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback
     ```
     > 將 `epyykzxxglkjombvozhr` 替換為您的 Supabase 專案引用 ID
   - 點擊 **「儲存」**
8. 獲取 Channel ID 和 Channel Secret：
   - 在 Channel 設定頁面中，您會看到：
     - **Channel ID**：複製此值
     - **Channel Secret**：點擊 **「顯示」** 來查看並複製

#### 步驟 5.2：在 Supabase 中配置 LINE Provider

1. 登入 [Supabase Dashboard](https://app.supabase.com/)
2. 選擇您的專案
3. 導航到 **Authentication** > **Providers**
4. 找到 **LINE** 並點擊 **「Enable」** 或切換開關啟用
5. 填入以下資訊：
   - **Client ID (for OAuth)**: 從 LINE Developers Console 複製的 Channel ID
   - **Client Secret (for OAuth)**: 從 LINE Developers Console 複製的 Channel Secret
6. 點擊 **「Save」**

#### 步驟 5.3：LINE Login 權限範圍

LINE Login 預設會請求以下權限：
- `profile`: 獲取用戶基本資訊（名稱、頭像等）
- `openid`: OpenID Connect 認證
- `email`: 獲取用戶電子郵件（如果用戶已驗證）

這些權限已經足夠，無需額外配置。

#### 步驟 5.4：測試 LINE 登錄

1. 在應用中點擊「使用 LINE 登入」按鈕
2. 應該會跳轉到 LINE 登錄頁面
3. 使用 LINE 帳號登錄
4. 授權應用後，應該會重定向回應用

**注意**：LINE Login 需要應用程式通過審核才能在生產環境中使用。開發階段可以在 LINE Developers Console 中設定測試環境。

---

## 🔍 查找 Supabase 專案引用 ID

1. 登入 Supabase Dashboard
2. 選擇您的專案
3. 導航到 **Settings** > **API**
4. 在「Project URL」中可以找到您的專案引用 ID
   - 例如：`https://abcdefghijklmnop.supabase.co`
   - 專案引用 ID 就是：`abcdefghijklmnop`

---

## ✅ 測試第三方登錄

### 測試步驟：

1. **確保前端代碼已正確配置**（已完成）：
   - `src/pages/AuthPage.tsx` 中已有 `handleSocialLogin` 函數
   - UI 按鈕已存在

2. **測試 Google 登錄**：
   - 點擊「使用 Google 登入」按鈕
   - 應該會跳轉到 Google 登錄頁面
   - 登錄成功後應該會重定向回應用

3. **測試 Apple 登錄**：
   - 點擊「使用 Apple 登入」按鈕
   - 應該會跳轉到 Apple 登錄頁面
   - 登錄成功後應該會重定向回應用

4. **測試 Discord 登錄**：
   - 點擊「使用 Discord 登入」按鈕
   - 應該會跳轉到 Discord 授權頁面
   - 點擊「Authorize」授權應用
   - 登錄成功後應該會重定向回應用

5. **測試 Facebook 登錄**：
   - 點擊「使用 Facebook 登入」按鈕
   - 應該會跳轉到 Facebook 登錄頁面
   - 登錄成功後應該會重定向回應用

6. **測試 LINE 登錄**：
   - 點擊「使用 LINE 登入」按鈕
   - 應該會跳轉到 LINE 登錄頁面
   - 登錄成功後應該會重定向回應用

---

## 🐛 常見問題

### 問題 1：重定向 URI 不匹配

**錯誤訊息**：`redirect_uri_mismatch`

**解決方案**：
- 確保在 OAuth provider（Google/Apple/Facebook/Discord）中配置的重定向 URI 與 Supabase 專案 URL 完全匹配
- 格式必須是：`https://<project-ref>.supabase.co/auth/v1/callback`

### 問題 2：Apple Sign In 需要 HTTPS

**說明**：
- Apple Sign In 只能在 HTTPS 環境下運作
- 本地開發時需要使用 Supabase 的本地開發環境或使用 ngrok 等工具

### 問題 3：Apple Developer 帳號需要付費

**說明**：
- Apple Sign In 需要 Apple Developer Program 會員資格（每年 $99 USD）
- 如果沒有付費帳號，可以暫時跳過 Apple 登錄，只使用 Google 登錄

### 問題 4：本地開發環境重定向問題

**解決方案**：
- 在本地開發時，重定向 URL 應該是：
  ```
  http://localhost:8080/home
  ```
- 確保在 `AuthPage.tsx` 中的 `redirectTo` 設定正確：
  ```typescript
  redirectTo: `${window.location.origin}/home`
  ```

---

## 📝 前端代碼檢查

前端代碼已經正確實現，位於 `src/pages/AuthPage.tsx`：

```typescript
const handleSocialLogin = async (provider: 'google' | 'apple' | 'discord' | 'facebook' | 'line') => {
  try {
    const { error } = await supabase.auth.signInWithOAuth({
      provider,
      options: {
        redirectTo: `${window.location.origin}/home`,
      },
    });

    if (error) {
      const providerNames: Record<string, string> = {
        'google': 'Google',
        'apple': 'Apple',
        'discord': 'Discord',
        'facebook': 'Facebook',
        'line': 'LINE'
      };
      const providerName = providerNames[provider] || provider;
      toast.error(getText('auth_social_login_error', `${providerName}登入失敗`));
    }
  } catch (error) {
    toast.error(getText('auth_login_error', '登入失敗，請稍後再試'));
  }
};
```

前端 UI 已包含以下第三方登錄按鈕：
- ✅ Google 登錄按鈕
- ✅ Apple 登錄按鈕
- ✅ Discord 登錄按鈕
- ✅ Facebook 登錄按鈕（新增）
- ✅ LINE 登錄按鈕（新增）

---

## 🎯 快速開始

### 選項 1：Discord 登錄（最簡單，推薦）⭐

1. **Discord Developer Portal**：
   - 前往 https://discord.com/developers/applications
   - 創建新應用程式
   - OAuth2 > 添加 Redirect URL：`https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback`
   - 複製 CLIENT ID 和 CLIENT SECRET

2. **Supabase Dashboard**：
   - Authentication > Providers > Discord
   - 啟用並填入 Client ID 和 Client Secret
   - Save

3. **測試**：
   - 在應用中點擊「使用 Discord 登入」
   - 應該可以正常運作

### 選項 2：LINE 登錄（台灣、日本、泰國等地區推薦）⭐

1. **LINE Developers Console**：
   - 前往 https://developers.line.biz/console/
   - 創建 Provider 和 LINE Login Channel
   - 添加 Callback URL：`https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback`
   - 複製 Channel ID 和 Channel Secret

2. **Supabase Dashboard**：
   - Authentication > Providers > LINE
   - 啟用並填入 Channel ID 和 Channel Secret
   - Save

3. **測試**：
   - 在應用中點擊「使用 LINE 登入」
   - 應該可以正常運作

### 選項 3：Google 登錄

如果您只想快速測試 Google 登錄：

1. **Google Cloud Console**：
   - 創建 OAuth 2.0 憑證
   - 添加重定向 URI：`https://<your-project-ref>.supabase.co/auth/v1/callback`

2. **Supabase Dashboard**：
   - Authentication > Providers > Google
   - 啟用並填入 Client ID 和 Client Secret
   - Save

3. **測試**：
   - 在應用中點擊「使用 Google 登入」
   - 應該可以正常運作

### 選項 4：Facebook 登錄

1. **Facebook Developers**：
   - 前往 https://developers.facebook.com/
   - 創建新應用程式
   - 添加 Facebook Login 產品
   - 配置 OAuth Redirect URI：`https://epyykzxxglkjombvozhr.supabase.co/auth/v1/callback`
   - 複製 App ID 和 App Secret

2. **Supabase Dashboard**：
   - Authentication > Providers > Facebook
   - 啟用並填入 App ID 和 App Secret
   - Save

3. **測試**：
   - 在應用中點擊「使用 Facebook 登入」
   - 應該可以正常運作

---

## 📚 參考資源

- [Supabase Auth Documentation](https://supabase.com/docs/guides/auth)
- [Google OAuth Setup](https://supabase.com/docs/guides/auth/social-login/auth-google)
- [Apple Sign In Setup](https://supabase.com/docs/guides/auth/social-login/auth-apple)
- [Facebook Login Setup](https://supabase.com/docs/guides/auth/social-login/auth-facebook)
- [Discord OAuth Setup](https://supabase.com/docs/guides/auth/social-login/auth-discord)

