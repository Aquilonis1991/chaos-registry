# 後台「系統配置」變數功能對照表

本表彙整所有 `system_config` 變數、對應功能與說明。

---

## 儲值 (recharge)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **recharge_amounts** | 美金儲值對應代幣數量，如 `{"1": 100, "5": 550}` | RechargePage、verify-app-store-purchase、verify-google-play-purchase EF | 修改時同步檢查 IAP 產品 ID 對應 |

---

## 驗證限制 (validation)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **title_max_length** | 主題標題最大字元數 | CreateTopicPage、create-topic EF | 與 title_min_length 搭配 |
| **title_min_length** | 主題標題最小字元數 | CreateTopicPage、create-topic EF | - |
| **description_max_length** | 主題描述最大字元數 | CreateTopicPage、create-topic EF | - |
| **option_min_count** | 投票選項最少數量 | CreateTopicPage、create-topic EF | 建議 ≥2 |
| **option_max_count** | 投票選項最多數量 | CreateTopicPage、create-topic EF | - |
| **option_item_max_length** | 單一選項最大字元數 | CreateTopicPage、create-topic EF | - |
| **tags_max_count** | 主題標籤最多數量 | CreateTopicPage、create-topic EF | - |
| **topic_banned_check_levels** | 主題**標題／選項／標籤**禁字檢查等級，JSON 陣列如 `["A","B","C","D","E"]` | CreateTopicPage、create-topic EF、validate_topic_content RPC | 與禁字表等級對應 |
| **topic_description_banned_levels** | 主題**詳述（description）**專用禁字等級；可與標題分開（例如描述較嚴） | CreateTopicPage（`checkBannedWords(description, …)`） | migration `20260320130000` 已寫入預設；後台可與 `topic_banned_check_levels` 設成相同或更嚴 |
| **nickname_banned_check_levels** | 暱稱禁字檢查等級 | ProfilePage 暱稱編輯 | - |

**變數說明（topic_description_banned_levels）**  
- 用途：僅影響「主題詳述」欄位前端 `checkBannedWords`；標題與選項仍用 `topic_banned_check_levels`。  
- 若兩者設為相同 JSON，行為與「全部共用一組等級」一致。

---

## 投票 (voting)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **vote_button_amounts** | 投票按鈕可選數量陣列 | VoteDetailPage | JSON 陣列如 `[1, 5, 10, 20, 50, 100]` |
| **vote_max_amount** | 單次投票最大數量 | RPC 驗證 | - |
| **vote_min_amount** | 單次投票最小數量 | RPC 驗證 | - |
| **free_vote_enabled** | 是否啟用每日免費票 | increment_option_votes RPC | true/false |
| **free_vote_per_topic_per_day** | 每主題每日免費票數量 | increment_option_votes RPC | - |

---

## 主題成本 (topic_cost)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **exposure_costs** | 曝光方案代幣成本 | CreateTopicPage、useTopicOperations、create-topic EF | key 須與 exposure_level 一致 |
| **duration_costs** | 投票天數對應額外代幣 | CreateTopicPage、useTopicOperations、create-topic EF | - |
| **duration_min_days** | 投票最少天數 | CreateTopicPage | - |
| **duration_max_days** | 投票最多天數 | CreateTopicPage | - |
| **create_topic_base_cost** | 發起主題基礎代幣費用 | CreateTopicPage、useTopicOperations | 0 表示免費 |
| **daily_topic_discount_tokens** | 每日折扣可折抵代幣數 | CreateTopicPage、useTopicOperations | - |
| **daily_free_topic_enabled** | 是否啟用每日首篇免費發起 | can_use_daily_free_topic、create-topic EF | true/false |
| **daily_free_topic_quota** | 每日免費發起主題次數上限 | can_use_daily_free_topic | 通常為 1 |
| **free_create_enabled** | 是否啟用任務/連續登入免費發起資格 | has_free_create_qualification RPC | 與 free_create_daily_login_days 搭配 |
| **free_create_qualification_expire_hours** | 免費發起資格過期時數 | 免費資格邏輯 | - |
| **extend_topic_1_day_cost** | 參與者延長投票 +1 天代幣成本 | extend_topic_voting_days RPC、quote RPC | - |
| **extend_topic_2_day_cost** | 參與者延長投票 +2 天代幣成本 | 同上 | - |
| **extend_topic_3_day_cost** | 參與者延長投票 +3 天代幣成本 | 同上 | - |
| **add_topic_option_cost** | 參與者新增投票選項代幣成本 | add_topic_option RPC、quote RPC | - |
| **topic_time_extension_max_per_topic** | 每主題最多可延長次數 | extend RPC | - |
| **topic_time_extension_max_days_per_action** | 每次延長最多天數 | extend RPC | - |
| **topic_time_extension_only_when_remaining_hours_leq** | 僅剩餘時間 ≤ N 小時時可延長 | extend RPC | - |
| **topic_time_extension_max_per_user** | 每位使用者每主題最多延長次數 | extend RPC | - |
| **topic_option_add_max_per_topic** | 每主題最多可新增選項數 | add_option RPC | - |
| **topic_option_add_max_per_user** | 每位使用者每主題最多新增選項數 | add_option RPC | - |
| **topic_option_add_min_length** | 新增選項最少字元 | add_option RPC | - |
| **topic_option_add_max_length** | 新增選項最多字元 | add_option RPC | - |

---

## 觀點角鬥場 (arena)

> **已廢棄分類 `battlefield`（應自 DB 刪除）**：程式與 RPC 皆只讀 `category = arena`。請執行 migration `20260321130000_merge_battlefield_system_config_to_arena.sql`：誤標之 `arena_*` 合併至 `arena`，其餘 `battlefield` 列 **DELETE**；後台 `SystemConfigManager` 已忽略該分類，不再顯示分頁。

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **arena_elite_min_threshold_y** | 進駐精英區所需最低淨贊同 | ArenaSection、purge_arena_on_topic_end | - |
| **arena_throne_min_threshold_x** | 登基核心區所需淨贊同門檻 | ArenaSection、purge_arena_on_topic_end | - |
| **arena_mundane_access_votes** | 該話題投票達此數才可發表觀點 | post_arena_message RPC | - |
| **arena_base_data_ttl** | 觀點初始 TTL（分鐘） | post_arena_message RPC | - |
| **arena_natural_decay_rate** | 每分鐘 TTL 自然消耗量 | decay_arena_ttl RPC（排程） | - |
| **arena_upvote_time_bonus** | 贊同按鈕增加 TTL（分鐘） | ArenaSection、cast_arena_vote RPC | - |
| **arena_downvote_time_penalty** | 斥責按鈕減少 TTL（分鐘） | ArenaSection、cast_arena_vote RPC | - |
| **arena_comment_max_length** | 觀點字數上限 | ArenaSection、post_arena_message RPC | - |
| **arena_shield_price** | 購買數據鎖定保險所需代幣 | ArenaSection、post_arena_message RPC | - |
| **arena_shield_duration_hours** | 鎖定時長（小時） | post_arena_message RPC | - |
| **arena_shield_legacy_bonus** | 購買保險額外給予 TTL（分鐘） | post_arena_message RPC | - |
| **arena.toast.onePerTopic**（`ui_texts`） | RPC「每人每主題僅一則」錯誤之使用者提示（對應 `One message per topic allowed`） | ArenaSection 發表失敗 toast | **20260320180000** |

**前台 UI（ArenaSection）**：空狀態與「投票截止時間」同用 `Card` + `bg-muted/50`、左 `text-muted-foreground`／右 `font-semibold`；無留言時仍顯示「發表觀點」（已登入且話題未結束）。UI 文案鍵含 `arena.empty`、`arena.emptyRowLabel`、`arena.loading`、`arena.toast.onePerTopic` 等；相關 migration 寫入 `ui_texts`。

---

## 首頁 (home)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **home_promoted_limit** | 首頁推廣區一次顯示主題數量 | HomePage | - |
| **home_expired_topic_grace_days** | 主題截止後仍保留在列表的天數 | useTopics、get_hot_topics、get_latest_topics RPC | - |
| **home_exposure_global_limit_normal** | 普通曝光每日全平台名額（0=無上限） | 曝光 RPC | - |
| **home_exposure_global_limit_medium** | 中等曝光每日全平台名額 | 同上 | - |
| **home_exposure_global_limit_high** | 高度曝光每日全平台名額 | 同上 | - |

---

## 廣告 (advertising)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **ad_insertion_interval** | 每 N 個主題插入 1 個廣告 | HomePage、SystemConfigManager 測試按鈕 | - |
| **ad_insertion_skip_first** | 首屏跳過主題數（不顯示廣告） | HomePage | - |
| **ad_insertion_enabled** | 是否啟用主題列表穿插廣告 | HomePage | true/false |
| **admob_native_ad_unit_id** | AdMob 原生廣告單元 ID，可為 `{android, ios}` | HomePage | - |
| **admob_rewarded_ad_unit_id** | 獎勵廣告單元 ID | useMissionOperations | - |

---

## 任務 (mission)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **mission_watch_ad_limit** | 每日觀看廣告次數上限 | useMissionOperations、watch-ad EF | 已統一，不再使用 `max_ads_per_day` |
| **mission_watch_ad_reward** | 每次觀看廣告獎勵代幣 | useMissionOperations、watch-ad EF | 已統一，不再使用 `ad_reward_amount` |
| **mission_create_topic_reward** | 發起主題任務獎勵代幣 | MissionPage、record_* RPC | - |
| **mission_vote_reward** | 投票愛好者任務獎勵代幣 | 任務完成邏輯 | - |
| **mission_first_vote_reward** | 新手上路（首次投票）獎勵 | MissionPage | - |
| **mission_vote_lover_target** | 投票愛好者目標次數 | MissionPage、record_* RPC | - |
| **mission_vote_lover_reward** | 投票愛好者獎勵代幣 | MissionPage | - |
| **mission_7days_login_target** | 連續登入目標天數 | record_* RPC | 已取代舊 key `consecutive_login_target` |
| **mission_7days_login_reward_tokens** | 連續登入獎勵代幣 | MissionPage | - |
| **mission_daily_login_reward** | 每日簽到獎勵代幣 | MissionPage、record_daily_login RPC | 已取代舊 key `daily_login_reward` |
| **free_create_daily_login_days** | 連續登入幾天獲得免費發起資格 | record_daily_login、free_create 邏輯 | - |

---

## 用戶 (user)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **irrational_assessment_cost** | 不理性鑑定代幣費用（每週首次免費） | ProfilePage、ai-user-classification EF | - |
| **new_user_tokens** | 新用戶註冊獲得初始代幣 | 註冊流程 RPC | - |

---

## 檢舉 (report)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **report_auto_hide_threshold** | 主題被**不同用戶**檢舉達此**人數**時，自動隱藏主題（與 `topics.report_count`、RPC 邏輯一致） | `handle_topic_report` RPC | migration `20260320130000` 已改為從本值讀取，預設 10；後台可調整閾值 |

**變數說明（report_auto_hide_threshold）**  
- 數字 N：當該主題的檢舉紀錄達到「至少 N 位不同檢舉者」且累計計數達標時，觸發自動隱藏。  
- 先前版本在函數內寫死 `10`；現已改為讀取 `system_config`，修改後不需改程式。

---

## 公告顯示 (announcement)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **announcement_max_display** | 前台輪播／列表一次向 `get_active_announcements` 請求的**最多筆數**（例如 3 則） | AnnouncementCarousel | 前端依此值呼叫 RPC；預設 3、上限 50 |

**變數說明（announcement_max_display）**  
- 僅影響「一次撈幾則有效公告」，不影響單則公告標題/內容長度（資料表仍有 CHECK 限制）。  
- 先前前端寫死 `3`；現已改為讀取 `system_config`。

**同分頁：公告編輯（`announcements` 資料表）**  
後台「系統配置」→ **公告顯示** 分頁下方內嵌 **AnnouncementManager**（`embedded`），可新增/編輯／刪除公告，欄位包含：  
- **分類**：重要、一般、節慶、活動、其他（`announcement_category`）  
- **背景配色**：8 種預設（`style_preset` 1～8，前台 `AnnouncementCarousel` 套用）  
- **顯示日期**：選填（`display_date`，未填則前台顯示建立日）  
- **標題／內文**、**權重** 1～100（`priority`）、**排程** `start_date`／`end_date`（開始後顯示、結束後下架；`get_active_announcements` 與 RLS 一致）  
請執行 migration **`20260321140000_announcement_category_style_display.sql`** 以新增欄位並更新 RPC。

---

## App 版本 (app)

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **app_min_version_android** | Android 最低版本號（如 1.0.48）。留空則不強制更新。僅原生 App 會檢查。 | useForceUpdate | - |
| **app_min_version_ios** | iOS 最低版本號（如 1.0.48）。留空則不強制更新。僅原生 App 會檢查。 | useForceUpdate | - |
| **app_store_url_android** | Android 商店連結 | useForceUpdate | - |
| **app_store_url_ios** | iOS App Store 連結 | useForceUpdate | - |

---

## 法律 (legal) — 後台「條款管理」分頁管理

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **legal_terms_content** | 使用者條款內容 | TermsPage、LegalPage、LegalContentManager | - |
| **legal_privacy_content** | 隱私權政策（繁中） | PrivacyPage、LegalPage、LegalContentManager | - |
| **legal_privacy_content_jp** | 隱私權政策（日文） | PrivacyPage (/privacy/jp)、LegalContentManager | - |
| **legal_privacy_content_en** | 隱私權政策（英文） | PrivacyPage (/privacy/en)、LegalContentManager | - |

---

## AI (ai) — 後台「AI 管理」分頁管理

| Key | 對應功能敘述 | 使用處 | 建議 |
|-----|--------------|--------|------|
| **ai_chaos_rewrite_prompt** | 不穩定改寫 AI 提示詞 | ai-chaos-rewrite EF、AiPromptManager | - |
| **ai_chaos_verification_prompt** | 不理性鑑定 AI 提示詞 | ai-user-classification EF、AiPromptManager | - |
| **ai_closing_prompt** | 主題結尾 AI 提示詞 | generate-ai-closing EF、AiPromptManager | - |
| **ai_chaos_rewrite_cost** | 不穩定改寫代幣費用 | ai-chaos-rewrite EF | - |

---

## 後台分頁對應

`SystemConfigManager` 主要分頁：  
validation、recharge、voting、topic_cost、arena、home、mission、advertising、user、**report**、**announcement**  

後台 `AdminPage` 頂層分頁（與系統配置同級）含：**公告顯示**（`AnnouncementManager`，編輯 `announcements` 資料表）、**觀點角鬥場**（`ArenaMessagesManager`，管理 `topic_arena_messages`：刪除、編輯內文、增減存在週期、增減贊同／斥責數；需 migration `20260321150000`）。

額外分頁：legal、app、ai  

**已廢棄、不顯示**：`battlefield`（後台已過濾；請執行 migration `20260321130000` 自資料庫刪除／合併殘留列）  

---

## Migration 紀錄

| 檔案 | 說明 |
|------|------|
| **20260320120000_remove_unused_system_config_keys.sql** | 刪除無程式讀取、未實作之變數（含當時的 report/announcement 冗餘項等） |
| **20260320130000_system_config_dedupe_and_wire_keys.sql** | 刪除重複舊 key（`consecutive_login_target`、`daily_login_reward`、`max_ads_per_day`、`ad_reward_amount`）；寫入 `topic_description_banned_levels`；還原並串接 `report_auto_hide_threshold`、`announcement_max_display`；更新 `handle_topic_report` |
| **20260321130000_merge_battlefield_system_config_to_arena.sql** | 移除 `battlefield`：`arena_*` 誤標者改為 `category = arena`；其餘該分類列 `DELETE`；套用後資料庫不再含此分類 |
| **20260321140000_announcement_category_style_display.sql** | `announcements` 新增 `announcement_category`、`style_preset`、`display_date`；權重 `priority` 改為 1～100；`get_active_announcements` 回傳分類／配色／顯示日 |
| **20260321150000_admin_arena_messages_rls.sql** | 管理員對 `topic_arena_messages`：可查全部（含已回收）、`UPDATE`、`DELETE`（後台 `ArenaMessagesManager`） |
| **20260321160000_option_item_max_length_category_validation.sql** | `option_item_max_length` 的 `category` 改為 `validation`（後台「驗證限制」分頁；舊環境曾為 `topic_limits`） |
| **20260321170000_remove_app_min_version_and_battlefield_keys.sql** | 刪除 `app_min_version`（已由平台 key 取代）；刪除 `battlefield_breath_boost_amount`、`battlefield_breath_boost_cost`、`battlefield_mundane_decay_per_minute`、`battlefield_throne_decay_per_minute`（程式無讀取） |

---

## 已刪除且未再啟用之變數（僅 20260320120000）

以下曾自資料庫移除，**未**在 20260320130000 復原（維持移除）：

- report_email_notifications  
- report_admin_email  
- report_require_auth  
- announcement_title_max_length  
- announcement_content_max_length  
- announcement_summary_max_length  
- announcement_auto_deactivate  
- ai_official_summary_prompt  

**註：** `report_auto_hide_threshold` 與 `announcement_max_display` 已在 **20260320130000** 重新建立並接上程式／RPC。
