# Edge Function 和 RPC 超時問題解決方案

## 🔍 問題分析

從日誌分析，發現以下問題：

### 觀察到的超時
- **Edge Function 超時**：8 秒後超時（已調整為 20 秒）
- **RPC 調用超時**：5 秒後超時（已調整為 15 秒）
- **最終結果**：兩個都超時，導致觀看廣告失敗

### 可能的原因

1. **Edge Function 未重新部署** ⚠️ **最可能**
   - 如果 Edge Function 沒有重新部署，仍然使用舊代碼（4次串行查詢）
   - 舊代碼執行時間長，容易超時

2. **網絡連接問題**
   - 設備網絡連接不穩定
   - Supabase 服務響應慢

3. **數據庫響應慢**
   - 數據庫負載高
   - 查詢執行時間長

---

## ✅ 已實施的修復

### 1. 增加超時時間
- **Edge Function**：從 8 秒增加到 **20 秒**
- **RPC**：從 5 秒增加到 **15 秒**

**原因**：
- 給足夠時間讓 Edge Function 和 RPC 完成
- 如果網絡慢，20 秒應該足夠

### 2. 改善錯誤訊息
- 添加更詳細的錯誤提示
- 區分超時錯誤和其他錯誤
- 提供用戶友好的錯誤訊息

---

## 🚀 必須執行的步驟

### 步驟 1：重新部署 Edge Function（**最重要**）

這是**最關鍵**的步驟。如果 Edge Function 沒有重新部署，仍然使用舊代碼（4次串行查詢），會導致超時。

#### 方法 A：使用 Supabase CLI

```powershell
cd C:\Users\USER\Documents\Mywork\votechaos-main

# 1. 確認已登入
supabase login

# 2. 連接專案（如果還沒連接）
supabase link --project-ref idfqzcsxvuxperxfieam

# 3. 部署 watch-ad Edge Function
supabase functions deploy watch-ad
```

#### 方法 B：使用 Supabase Dashboard

1. 前往：https://supabase.com/dashboard
2. 選擇您的專案
3. 左側選單 → **Edge Functions**
4. 找到 `watch-ad` 函數並點擊
5. 複製 `votechaos-main\supabase\functions\watch-ad\index.ts` 的內容
6. 貼上到 Dashboard 編輯器
7. 點擊 **Deploy** 按鈕

**詳細步驟**：請參考 `重新部署watch-ad Edge Function詳細步驟.md`

### 步驟 2：確認後台配置

在 Supabase Dashboard 中檢查 `system_config` 表：

1. 確認 `mission_watch_ad_reward` 是否存在
2. 如果不存在，創建它：
   ```sql
   INSERT INTO system_config (key, value, category, description)
   VALUES ('mission_watch_ad_reward', 5, 'mission', '觀看廣告獲得的代幣數量');
   ```

### 步驟 3：重新構建並同步（已完成）

```powershell
npm run build
npx cap sync android
```

### 步驟 4：在 Android Studio 中刷新

1. 同步 Gradle：點擊 🔄 Sync Project with Gradle Files
2. 清理並重新構建：`Build` → `Clean Project` → `Rebuild Project`
3. 重新運行應用

---

## 🔍 驗證部署是否成功

### 方法 1：檢查 Dashboard

1. 前往 Supabase Dashboard → Edge Functions → watch-ad
2. 查看 **Last updated** 時間
3. 確認時間是剛才部署的時間

### 方法 2：查看函數代碼

在 Dashboard 中查看 `watch-ad` 函數的代碼，確認：
- 配置讀取使用 **1次並行查詢**（不是 4 次串行查詢）
- 代碼包含以下內容：
  ```typescript
  // 從 system_config 讀取配置（一次性讀取所有需要的配置，避免多次查詢）
  const { data: configData, error: configError } = await supabaseClient
    .from('system_config')
    .select('key, value')
    .in('key', ['mission_watch_ad_limit', 'max_ads_per_day', 'mission_watch_ad_reward', 'ad_reward_amount']);
  ```

---

## 📊 預期改善

部署成功後，您應該看到：

1. **配置讀取優化**：
   - Edge Function 內部配置讀取從 4 次查詢減少到 1 次
   - 配置讀取時間從 ~400ms 減少到 ~100ms

2. **響應時間改善**：
   - Edge Function 在 20 秒內完成（或超時後使用 RPC）
   - RPC 在 15 秒內完成
   - 總響應時間：< 20 秒（如果 Edge Function 成功）或 < 35 秒（如果使用 RPC）

3. **錯誤處理改善**：
   - 超時錯誤顯示更詳細的提示
   - 用戶知道是網絡問題，可以稍後再試

---

## ⚠️ 如果問題仍然存在

如果完成上述步驟後，問題仍然存在，請檢查：

### 1. Edge Function 是否已重新部署
- 檢查 Supabase Dashboard → Edge Functions → watch-ad
- 確認 **Last updated** 時間是最新的
- 確認代碼已更新（使用 1 次並行查詢）

### 2. 網絡連接
- 檢查設備網絡連接
- 確認 Supabase 服務是否正常
- 嘗試使用其他網絡（WiFi/移動數據）

### 3. 數據庫性能
- 檢查 Supabase Dashboard → Database → Performance
- 確認數據庫響應時間正常
- 檢查是否有長時間運行的查詢

### 4. 查看詳細日誌
- 在 Android Studio 的 Logcat 中查看詳細日誌
- 確認 Edge Function 和 RPC 的實際執行時間
- 查看是否有其他錯誤訊息

---

## 📝 測試檢查清單

完成修復後，請測試：

- [ ] Edge Function 已重新部署（Dashboard 顯示最新時間）
- [ ] 配置讀取正確（使用後台設置的 `mission_watch_ad_reward`）
- [ ] Edge Function 在 20 秒內完成（或超時後使用 RPC）
- [ ] RPC 在 15 秒內完成
- [ ] 第一次觀看廣告：響應時間 < 20 秒
- [ ] 第二次觀看廣告：響應時間 < 20 秒
- [ ] 不會出現超時錯誤
- [ ] 如果超時，顯示友好的錯誤訊息

---

## 🔗 相關文檔

- [重新部署watch-ad Edge Function詳細步驟.md](./重新部署watch-ad Edge Function詳細步驟.md)
- [性能優化修復說明.md](./性能優化修復說明.md)
- [刷新Android專案流程.md](./刷新Android專案流程.md)



