# 重新啟動開發伺服器 - 指令

## ✅ 環境變數已確認

`.env.local` 檔案已包含正確的配置：
- ✅ `VITE_SUPABASE_URL` 已設置
- ✅ `VITE_SUPABASE_PUBLISHABLE_KEY` 已設置

---

## 🚀 重新啟動開發伺服器

### 步驟 1：停止當前開發伺服器（如果正在運行）

**方法 1：在運行開發伺服器的終端視窗中**
- 按 `Ctrl + C` 停止

**方法 2：使用 PowerShell 命令**
```powershell
# 停止所有 Node.js 進程（謹慎使用，會停止所有 Node 進程）
Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force
```

---

### 步驟 2：重新啟動開發伺服器

在專案根目錄執行：

```powershell
cd C:\Users\USER\Documents\Mywork\votechaos-main
npm run dev
```

---

## ✅ 驗證

啟動開發伺服器後：

1. **檢查終端輸出**：
   - 應該看到類似 `VITE v5.x.x  ready in xxx ms` 的訊息
   - 應該顯示本地開發 URL（通常是 `http://localhost:5173`）

2. **檢查瀏覽器控制台**：
   - 打開開發者工具（F12）
   - 進入 Console 標籤
   - 確認沒有環境變數相關的錯誤訊息

3. **測試 X 登入**：
   - 點擊 X (Twitter) 登入按鈕
   - 確認不再出現 401 錯誤
   - 確認可以正常跳轉到 X 授權頁面

---

## 📝 快速指令

**一鍵重新啟動**（在 PowerShell 中執行）：

```powershell
cd C:\Users\USER\Documents\Mywork\votechaos-main
# 停止現有進程（如果存在）
Get-Process -Name node -ErrorAction SilentlyContinue | Where-Object { $_.Path -like "*votechaos-main*" } | Stop-Process -Force -ErrorAction SilentlyContinue
# 等待一秒
Start-Sleep -Seconds 1
# 啟動開發伺服器
npm run dev
```

---

**環境變數已配置完成，請重新啟動開發伺服器！**
