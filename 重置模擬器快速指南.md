# 重置 Android 模擬器快速指南

**目標**: 重置模擬器，確保所有修改都應用到應用  
**預計時間**: 5-10 分鐘

---

## 🚀 快速重置步驟（一鍵執行）

### Windows PowerShell

```powershell
# 在專案根目錄執行
.\reset-android.ps1
```

### Linux/Mac

```bash
# 在專案根目錄執行
chmod +x reset-android.sh
./reset-android.sh
```

---

## 📋 手動重置步驟（逐步執行）

### 步驟 1: 清理應用數據

```bash
adb shell pm clear com.votechaos.app.debug
```

**預期輸出**: `Success`

---

### 步驟 2: 清理並重新構建前端

```bash
cd votechaos-main
npm run build
```

**預期輸出**: 
```
vite v5.x.x building for production...
✓ built in Xs
```

---

### 步驟 3: 同步 Capacitor

```bash
npm run cap:sync:android
```

**預期輸出**:
```
✔ Copying web assets from dist to android/app/src/main/assets/public
✔ Copying native bridge
✔ Copying capacitor.config.json
✔ Syncing Android project
```

---

### 步驟 4: 清理 Android 構建

```bash
cd android
./gradlew clean
```

**Windows**:
```powershell
.\gradlew.bat clean
```

**預期輸出**:
```
BUILD SUCCESSFUL in Xs
```

---

### 步驟 5: 重新構建並安裝

```bash
./gradlew assembleDebug installDebug
```

**Windows**:
```powershell
.\gradlew.bat assembleDebug installDebug
```

**預期輸出**:
```
BUILD SUCCESSFUL in Xs
Installing APK 'app-debug.apk' on 'emulator-XXXX' for 'app:debug'
Installed on 1 device.
```

---

### 步驟 6: 打開 Logcat

```bash
adb logcat -s VoteChaos
```

---

### 步驟 7: 啟動應用

**方法 A: 在模擬器中點擊應用圖標**

**方法 B: 使用命令**
```bash
adb shell am start -n com.votechaos.app.debug/.MainActivity
```

---

## ✅ 驗證檢查

### 構建驗證

- [ ] ✅ `npm run build` 成功
- [ ] ✅ `npm run cap:sync:android` 成功
- [ ] ✅ `./gradlew clean` 成功
- [ ] ✅ `./gradlew assembleDebug` 成功
- [ ] ✅ `./gradlew installDebug` 成功

### 應用驗證

- [ ] ✅ 應用已安裝（在模擬器應用列表中可見）
- [ ] ✅ 應用可以正常啟動
- [ ] ✅ Logcat 顯示正常日誌

---

## 🔄 完全重置（如果需要）

### 擦除模擬器數據

1. **在 Android Studio 中**
   - 打開 AVD Manager
   - 找到您的模擬器
   - 點擊「Wipe Data」按鈕（🗑️）
   - 確認擦除

2. **重新啟動模擬器**
   - 點擊「Play」按鈕啟動模擬器
   - 等待模擬器完全啟動

3. **重新執行重置步驟**

---

## 📝 重置後測試流程

### 1. 確認應用已安裝

```bash
adb shell pm list packages | grep votechaos
```

應該看到：
```
package:com.votechaos.app.debug
```

### 2. 打開 Logcat

```bash
adb logcat -s VoteChaos
```

### 3. 啟動應用

在模擬器中點擊 VoteChaos 應用圖標

### 4. 檢查初始日誌

應該看到：
```
[VoteChaos] MainActivity onCreate start
[VoteChaos] MainActivity onCreate complete
[VoteChaos] MainActivity onStart
[VoteChaos] MainActivity onResume
```

### 5. 開始測試 Twitter 登入

按照「Twitter 登入快速測試指南」進行測試

---

## ⚠️ 常見問題

### 問題 1: 模擬器未連接

**檢查**:
```bash
adb devices
```

**解決**:
- 確認模擬器已啟動
- 重啟 ADB: `adb kill-server && adb start-server`

### 問題 2: 構建失敗

**解決**:
```bash
# 清理 Gradle 緩存
cd android
rm -rf .gradle
rm -rf build
rm -rf app/build
./gradlew clean --refresh-dependencies
```

### 問題 3: 應用安裝失敗

**解決**:
```bash
# 卸載舊版本
adb uninstall com.votechaos.app.debug

# 重新安裝
cd android
./gradlew installDebug
```

---

## 🎯 重置完成標誌

### 成功標誌

- ✅ 所有命令執行成功
- ✅ 應用已安裝在模擬器上
- ✅ 應用可以正常啟動
- ✅ Logcat 顯示正常日誌
- ✅ 登入頁面正常顯示

---

**快速指南生成時間**: 2025年1月
