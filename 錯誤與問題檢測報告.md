# 🔍 VoteChaos 專案錯誤與問題檢測報告

**檢測時間**: 2025年1月  
**檢測範圍**: 邏輯錯誤、配置錯誤、潛在 Bug、安全問題

---

## 🚨 嚴重問題（必須立即修復）

### 1. ⚠️ **硬編碼的 API Key 在源代碼中**

**位置**: `src/integrations/supabase/client.ts:7`

**問題**:
```typescript
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

**風險**:
- 🔴 **高風險**: API Key 已暴露在源代碼中
- 即使有備援機制，硬編碼的 key 仍可能被濫用
- 如果此 key 被撤銷，應用將無法運行

**建議修復**:
```typescript
const SUPABASE_PUBLISHABLE_KEY = import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY;
if (!SUPABASE_PUBLISHABLE_KEY) {
  throw new Error('VITE_SUPABASE_PUBLISHABLE_KEY 環境變數未設置');
}
```

**優先級**: 🔴 **P0 - 立即修復**

---

### 2. ⚠️ **useAsyncAction Hook 的依賴問題**

**位置**: `src/hooks/useAsyncAction.tsx:41`

**問題**:
```typescript
const execute = useCallback(async <T,>(...), [loading]);
```

**風險**:
- 🟡 **中風險**: `loading` 在依賴數組中，但 `execute` 函數內部會修改 `loading`
- 可能導致無限循環或狀態不一致
- 每次 `loading` 變化都會重新創建 `execute` 函數

**建議修復**:
```typescript
const execute = useCallback(async <T,>(...), []); // 移除 loading 依賴
// 或使用 useRef 來追蹤 loading 狀態
```

**優先級**: 🟡 **P1 - 高優先級**

---

### 3. ⚠️ **AuthContext 中的閉包問題**

**位置**: `src/contexts/AuthContext.tsx:45-50`

**問題**:
```typescript
const timeoutTimer = setTimeout(() => {
  if (loading) {  // 這裡的 loading 是閉包中的舊值
    setLoading(false);
  }
}, 2000);
```

**風險**:
- 🟡 **中風險**: `setTimeout` 回調中的 `loading` 是閉包捕獲的舊值
- 如果組件在 2 秒內卸載，可能導致狀態更新到已卸載的組件

**建議修復**:
```typescript
const timeoutTimer = setTimeout(() => {
  setLoading(false); // 直接設置，不需要檢查
}, 2000);
```

**優先級**: 🟡 **P1 - 高優先級**

---

## 🟡 中風險問題

### 4. **樂觀更新與實時訂閱的潛在衝突**

**位置**: `src/hooks/useProfile.tsx:104-115` 和 `src/hooks/useVoteOperations.tsx:46`

**問題**:
- 投票時先進行樂觀更新（減少代幣）
- 實時訂閱可能會覆蓋樂觀更新
- 如果投票失敗，需要手動回滾，但實時訂閱可能已經更新了狀態

**風險**:
- 🟡 **中風險**: 可能導致代幣數量顯示不一致
- 用戶可能看到代幣先減少，然後又增加（如果投票失敗）

**建議**:
- 確保回滾邏輯正確執行
- 考慮在實時訂閱中忽略來自樂觀更新的變更

**優先級**: 🟡 **P2 - 中優先級**

---

### 5. **缺少錯誤邊界的清理**

**位置**: `src/components/ErrorBoundary.tsx`

**問題**:
- 錯誤邊界組件可能沒有正確清理資源
- 如果錯誤發生，某些訂閱或監聽器可能沒有被清理

**建議**:
- 確保在 `componentDidCatch` 中清理所有資源
- 添加 `componentWillUnmount` 清理邏輯

**優先級**: 🟡 **P2 - 中優先級**

---

### 6. **Promise.race 的潛在問題**

**位置**: `src/hooks/useMissionOperations.tsx:53-56` 和 `65-68`

**問題**:
```typescript
rpcResult = await Promise.race([
  rpcPromise,
  timeoutPromise
]) as { data: any; error: any };
```

**風險**:
- 🟡 **中風險**: 如果 `timeoutPromise` 先完成，`rpcPromise` 仍會繼續執行
- 可能導致資源浪費或狀態不一致
- 使用 `as any` 跳過了類型檢查

**建議**:
- 使用 `AbortController` 來取消請求
- 移除 `as any`，使用正確的類型

**優先級**: 🟡 **P2 - 中優先級**

---

### 7. **記憶體洩漏風險：事件監聽器**

**位置**: `src/lib/errorLogger.ts:242-259`

**問題**:
```typescript
export const setupGlobalErrorHandlers = () => {
  window.addEventListener('unhandledrejection', (event) => {...});
  window.addEventListener('error', (event) => {...});
  // 沒有返回清理函數
};
```

**風險**:
- 🟡 **中風險**: 如果多次調用 `setupGlobalErrorHandlers`，會添加多個監聽器
- 沒有清理機制，可能導致記憶體洩漏

**建議**:
```typescript
export const setupGlobalErrorHandlers = () => {
  const handleRejection = (event) => {...};
  const handleError = (event) => {...};
  
  window.addEventListener('unhandledrejection', handleRejection);
  window.addEventListener('error', handleError);
  
  return () => {
    window.removeEventListener('unhandledrejection', handleRejection);
    window.removeEventListener('error', handleError);
  };
};
```

**優先級**: 🟡 **P2 - 中優先級**

---

## 🟢 低風險問題（建議改進）

### 8. **過度使用 `as any`**

**位置**: 多處

**問題**:
- 大量使用 `as any` 跳過類型檢查
- 例如: `src/hooks/useVoteOperations.tsx:139`, `src/hooks/useMissionOperations.tsx:42`

**影響**:
- 失去 TypeScript 的類型安全保護
- 可能導致運行時錯誤

**建議**:
- 定義正確的類型
- 使用類型斷言而非 `as any`

**優先級**: 🟢 **P3 - 低優先級**

---

### 9. **console.log 過多**

**位置**: 全專案（598 個 console 調用）

**問題**:
- 生產環境中仍有大量 `console.log`
- 可能影響性能
- 可能洩露敏感信息

**建議**:
- 使用環境變數控制日誌輸出
- 生產環境移除或使用日誌服務

**優先級**: 🟢 **P3 - 低優先級**

---

### 10. **缺少 null 檢查**

**位置**: 多處，例如 `src/hooks/useVoteOperations.tsx:100-107`

**問題**:
```typescript
const foundOption = topic.options.find((opt: any) =>
  (opt.id === option) || (opt.id === undefined && opt === option)
);
if (foundOption) {
  optionLabel = typeof foundOption === 'string' ? foundOption : (foundOption.text || foundOption.label || option);
}
```

**風險**:
- 如果 `topic.options` 是 `null` 或 `undefined`，會導致運行時錯誤
- 雖然有 `Array.isArray` 檢查，但可能不夠

**建議**:
- 添加更嚴格的 null 檢查
- 使用可選鏈操作符

**優先級**: 🟢 **P3 - 低優先級**

---

### 11. **TypeScript 配置過於寬鬆**

**位置**: `tsconfig.json`

**問題**:
```json
{
  "noImplicitAny": false,
  "strictNullChecks": false,
  "noUnusedLocals": false,
  "noUnusedParameters": false
}
```

**影響**:
- 失去 TypeScript 的類型安全保護
- 可能隱藏潛在的錯誤

**建議**:
- 逐步啟用嚴格模式
- 先啟用 `strictNullChecks`

**優先級**: 🟢 **P3 - 低優先級**

---

### 12. **環境變數處理不一致**

**位置**: `src/integrations/supabase/client.ts`

**問題**:
- 開發環境和生產環境的處理邏輯不同
- 生產環境使用默認值可能導致問題

**建議**:
- 統一環境變數處理邏輯
- 生產環境應該拋出錯誤而非使用默認值

**優先級**: 🟢 **P3 - 低優先級**

---

## 🔒 安全問題

### 13. **API Key 暴露風險**

**位置**: `src/integrations/supabase/client.ts:6-7`

**問題**:
- Supabase URL 和 API Key 硬編碼在源代碼中
- 即使有環境變數備援，硬編碼的值仍可能被提取

**風險**:
- 🔴 **高風險**: 如果代碼被公開，API Key 可能被濫用
- 可能導致未授權訪問

**建議**:
- 移除所有硬編碼的 API Key
- 強制要求環境變數
- 使用環境變數驗證

**優先級**: 🔴 **P0 - 立即修復**

---

### 14. **缺少輸入驗證**

**位置**: 多處 API 調用

**問題**:
- 某些用戶輸入可能沒有充分驗證
- 雖然有 Zod schemas，但可能沒有覆蓋所有情況

**建議**:
- 確保所有用戶輸入都經過驗證
- 在後端也進行驗證（不依賴前端）

**優先級**: 🟡 **P1 - 高優先級**

---

## 🐛 潛在 Bug

### 15. **投票失敗時的回滾邏輯**

**位置**: `src/hooks/useVoteOperations.tsx:45-88`

**問題**:
- 樂觀更新在投票之前執行
- 如果投票失敗，需要回滾
- 但如果實時訂閱已經更新，回滾可能無效

**風險**:
- 🟡 **中風險**: 可能導致代幣數量不一致

**建議**:
- 確保回滾邏輯正確執行
- 考慮使用事務或更可靠的狀態管理

**優先級**: 🟡 **P2 - 中優先級**

---

### 16. **免費投票檢查的競態條件**

**位置**: `src/hooks/useVoteOperations.tsx:340-367`

**問題**:
- `checkFreeVoteAvailable` 和實際投票之間有時間窗口
- 用戶可能在檢查後、投票前使用免費票

**風險**:
- 🟡 **中風險**: 可能導致重複使用免費票

**建議**:
- 在後端函數中進行原子性檢查
- 使用資料庫鎖定機制

**優先級**: 🟡 **P2 - 中優先級**

---

### 17. **useEffect 依賴數組問題**

**位置**: `src/pages/VoteDetailPage.tsx:105-119`

**問題**:
```typescript
useEffect(() => {
  if (user && !isAnonymous && id) {
    checkFreeVoteAvailable(id)
      .then(setFreeVoteAvailable)
      ...
  }
}, [user, isAnonymous, id]); // checkFreeVoteAvailable 不在依賴中
```

**風險**:
- 🟢 **低風險**: 如果 `checkFreeVoteAvailable` 的實現改變，可能導致問題
- ESLint 可能會警告

**建議**:
- 將 `checkFreeVoteAvailable` 用 `useCallback` 包裝
- 或將其添加到依賴數組（如果穩定）

**優先級**: 🟢 **P3 - 低優先級**

---

## 📊 問題統計

| 嚴重程度 | 數量 | 優先級 |
|---------|------|--------|
| 🔴 嚴重 | 2 | P0 |
| 🟡 中風險 | 7 | P1-P2 |
| 🟢 低風險 | 8 | P3 |

**總計**: 17 個問題

---

## 🎯 修復建議優先順序

### 立即修復（本週）
1. ✅ 移除硬編碼的 API Key
2. ✅ 修復 useAsyncAction 的依賴問題
3. ✅ 修復 AuthContext 的閉包問題

### 高優先級（2週內）
4. ✅ 改善樂觀更新與實時訂閱的協調
5. ✅ 修復 Promise.race 的問題
6. ✅ 添加事件監聽器清理機制

### 中優先級（1個月內）
7. ✅ 減少 `as any` 的使用
8. ✅ 添加更嚴格的 null 檢查
9. ✅ 改善環境變數處理

### 低優先級（長期改進）
10. ✅ 啟用 TypeScript 嚴格模式
11. ✅ 清理 console.log
12. ✅ 改善錯誤處理

---

## 📝 總結

**整體評估**: 🟡 **中等風險**

專案整體結構良好，但存在一些需要修復的問題：

**優點**:
- ✅ 有錯誤處理機制
- ✅ 有實時訂閱機制
- ✅ 有樂觀更新機制

**需要改進**:
- ⚠️ 安全問題（API Key 硬編碼）
- ⚠️ 一些邏輯錯誤（依賴、閉包）
- ⚠️ 類型安全（過度使用 `as any`）

**建議**:
1. 立即修復安全問題
2. 逐步改善代碼質量
3. 啟用 TypeScript 嚴格模式（逐步進行）

---

**報告生成工具**: Cursor AI  
**最後更新**: 2025年1月
